# ─────────────────────────────────────────────────────────────────────
# PyTrack · Exemplo de variáveis de ambiente
# Copie este arquivo para `.env.local` e preencha com os seus valores.
#   cp .env.example .env.local
# NUNCA faça commit do .env / .env.local (contêm segredos).
# Variáveis NEXT_PUBLIC_* vão para o navegador; as demais são só do servidor.
# ─────────────────────────────────────────────────────────────────────

# ── App ──────────────────────────────────────────────────────────────
# URL canônica da aplicação (em produção: https://www.pytrack.com.br)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── Supabase ─────────────────────────────────────────────────────────
# Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://SEU-PROJETO.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ... (anon public key)
# Chave service_role (SERVIDOR — ignora RLS; use só em webhooks/admin/jobs)
SUPABASE_SERVICE_ROLE=eyJ... (service_role secret)
# (opcional) alias aceito pelo código:
# SUPABASE_SERVICE_ROLE_KEY=eyJ...

# ── Stripe ───────────────────────────────────────────────────────────
# Developers → API keys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_SECRET_KEY=sk_test_xxx            # ou restricted key rk_live_xxx
# Developers → Webhooks → (seu endpoint) → Signing secret
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Price IDs (Developers → Products → Prices)
STRIPE_PRICE_ID=price_xxx                 # Essencial mensal (R$10)
STRIPE_PRICE_ID_ANNUAL=price_xxx          # Essencial anual (R$96)
STRIPE_PRICE_ID_COMPLETO=price_xxx        # Completo mensal (R$19)
STRIPE_PRICE_ID_COMPLETO_ANNUAL=price_xxx # Completo anual (R$182)
STRIPE_PRICE_ID_SUPREMA=price_xxx         # Suprema mensal (R$46)
STRIPE_PRICE_ID_SUPREMA_ANNUAL=price_xxx  # Suprema anual (R$442)
STRIPE_PRICE_ID_VITALICIO=price_xxx       # Vitalício (pagamento único, R$697)

# Trial e indicações
STRIPE_TRIAL_DAYS=7
STRIPE_REFERRAL_COUPON=INDICACAO1MES      # id de um coupon criado na Stripe

# ── IA ───────────────────────────────────────────────────────────────
# Provider padrão: Nvidia NIM (se NVIDIA_API_KEY existir). Senão, OpenRouter.
# Usada em: chat IA, correção de exercícios, consultor de carreira, análise de projetos.
# (Cada usuário pode também configurar a própria IA em /configuracoes/ia.)
NVIDIA_API_KEY=nvapi-xxx                   # build.nvidia.com (provider padrão)
OPENROUTER_API_KEY=sk-or-v1-xxx            # openrouter.ai/keys (fallback)

# ── E-mail (Gmail SMTP) ──────────────────────────────────────────────
# Conta com verificação em 2 etapas + senha de app (myaccount.google.com/apppasswords)
GMAIL_USER=voce@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx     # senha de app (espaços são removidos)
CONTACT_TO=contato@seudominio.com          # destino dos e-mails de contato/suporte

# ── Supabase Management (backups + rota admin /admin/banco) ──────────
# Account → Access Tokens (sbp_...). Permite DDL e leitura de stats.
SUPABASE_MANAGEMENT_TOKEN=sbp_xxx
SUPABASE_PROJECT_REF=seu-project-ref       # ref do projeto (para o backup script)

# ── Analytics & Receita verificada ──────────────────────────────────
NEXT_PUBLIC_GA_ID=G-XXXXXXX                # Google Analytics 4 (Measurement ID)
GOOGLE_SITE_VERIFICATION=xxxxx             # verificação do Search Console (opcional)
DATAFAST_API_KEY=df_xxx                    # datafa.st (atribuição de receita)
TRUSTMRR_API_KEY=tmrr_xxx                  # trustmrr.com (receita verificada — admin)

# ── Segurança / anti-abuso ───────────────────────────────────────────
MAX_ACCOUNTS_PER_IP=2                       # máx. de contas por IP (anti-abuso do trial)

# ── Monitoramento (opcional) ─────────────────────────────────────────
# Destino externo de logs (Better Stack/Logtail, Discord/Slack webhook…)
# LOG_WEBHOOK_URL=https://...
