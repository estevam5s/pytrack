---
name: pytrack-platform
description: Architecture and key facts of the PyTrack Python learning dashboard project
metadata: 
  node_type: memory
  type: project
  originSessionId: e80488b4-200b-45f2-a1a1-9c9739d237ee
---

**PyTrack** — Next.js 15 + TS + Tailwind + Supabase learning dashboard at `/Users/estevamsouza/Documents/aulas/plataforma-python`.

- Supabase project ref `zohqgnhymtqppgzlammv` (name "Carreira"). Schema/seed are pushed via the **Management API** (`POST /v1/projects/{ref}/database/query`) using an `sbp_...` personal token the user provides — not committed. `mailer_autoconfirm` was enabled so signup logs in immediately. Demo login: `estudante.teste@pytrack.dev` / `python123`.
- Curriculum source is `doc/Conteudos/` (Parte-1..10, ~266 `.md` lessons). `scripts/generate-content.mjs` walks it and emits `lib/content/manifest.ts` (typed modules+lessons, **paths not bodies**) and `supabase/contents_docs.sql` (30 modules into `contents`, with `slug`/`lessons_count` columns). Re-run the script after editing `doc/Conteudos`, then push the SQL.
- Reader: `/conteudos` lists modules → `/conteudos/[modulo]` overview → `/conteudos/[modulo]/[licao]` renders markdown read **from disk at request time** (`lib/content/registry.ts`), so `doc/Conteudos` must ship with any deploy. Lesson completion is localStorage; module % syncs to Supabase `progress` via `setModuleProgressBySlug`.
- SQL files live in `supabase/` (schema.sql, seed.sql, contents_docs.sql, carreira_extra.sql, practice_exercises.sql, udemy_crud.sql). Catalog totals: 30 modules/266 lessons, 60 stack_items, 13 career_paths, **1230 practice_exercises** (table generated from `doc/Conteudos/exercicio_python.md` via `scripts/generate-exercises.mjs` → JSON → bulk REST insert with service role; `/exercicios` filters by 17 group_labels + level, completion in localStorage).
- `/perguntas-carreira-python`: `interview_questions` table (577 rows) generated from `doc/Conteudos/perguntas.md` via `scripts/generate-questions.mjs` (classifies each question into 13 tech categories by keyword) → JSON → REST insert. UI is a tabbed view (tech bank with category sidebar/search/expandable cards + Carreira FAQ). After creating a table via Management API, **PostgREST needs `notify pgrst, 'reload schema'`** before REST inserts work (else PGRST205). Tripled to **1719** questions via `scripts/generate-questions-v2.mjs` (252 curated + ~1142 ecosystem-coverage across ~190 techs); added a `seniority` column (junior/pleno/senior/especialista) — questions-view filters/orders by it (chips with counts, badge per card). **PostgREST caps responses at max-rows=1000**, so both `getPracticeExercises` and `getInterviewQuestions` must paginate via `.range()` loops (the stat showed "1000" before this fix).
- **The marketing site was MERGED into the single app** (the separate `site/` project was deleted) — because two apps on different localhost ports caused ERR_CONNECTION_REFUSED on cross-links and meant two Vercel deploys. Now: public site is route group `app/(site)/` (layout = navbar+footer) with pages `/ /sobre /trilhas /recursos /precos` (Projetos/Carreira are landing anchors `/#projetos`,`/#carreira` to avoid colliding with the dashboard's `/projetos`,`/carreira`). The **dashboard home moved from `/` to `/inicio`** (`app/(dashboard)/inicio/page.tsx`); nav "Início"→`/inicio`, sidebar-item active checks `/inicio`, auth actions redirect to `/inicio`, breadcrumb home links →`/inicio`, `revalidatePath("/")`→`/inicio`. Site components in `components/site/` (their Button is `components/site/site-button.tsx`, NOT the dashboard `ui/button`); data in `lib/site-data.ts`; links/brand in `lib/site-links.ts` (LOGIN_URL=`/auth/login`, SIGNUP_URL=`/auth/register` — internal). Site Tailwind tokens (green/blue/magenta/primary.light/surface-2/text-primary/`bg-brand`/float) added to the dashboard tailwind.config; `.card`/`.card-hover`/`.border-gradient`/`.bg-grid`/`.radial-fade` added to globals; `.text-gradient` now uses `bg-brand`. Middleware: public = `/`(exact)+`/sobre|/trilhas|/recursos|/precos|/auth`(prefix); everything else needs auth; logged-in users only redirected away from `/auth/*`→`/inicio` (NOT from `/`, so "Ir para o site" works). "Ir para o site" buttons (sidebar+user-menu) now link internally to `/`.
- **DEPLOYED to Vercel**: project `plataforma-python` (estevam5s-projects, linked to GitHub `estevam5s/analista-de-dados`), live at **https://plataforma-python.vercel.app**. Env vars set (Production/Preview/Development): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `OPENROUTER_API_KEY` (only these 3 are runtime; `SUPABASE_SERVICE_ROLE` is scripts-only, NOT on Vercel). Deploy gotchas hit & fixed: (1) **Vercel blocks Next 15.1.3 (CVE-2025-66478)** → upgraded to **next@15.5.19** (latest 15.x; package.json). (2) `.vercelignore` MUST anchor patterns with leading `/` (e.g. `/supabase`) — a bare `supabase` also excluded `lib/supabase/` and broke the build ("Can't resolve @/lib/supabase/client"). `.vercelignore` excludes `/.env*`, `/scripts`, `/supabase`, `/prompt.md`, `/informacao.md`. Future `git push` to main auto-deploys; or `vercel --prod` from local.
- **Vercel**: lesson markdown is read from disk at runtime, so `next.config.mjs` has `outputFileTracingIncludes` for `/conteudos/[modulo]` + `/conteudos/[modulo]/[licao]` → `./doc/Conteudos/**/*.md` (else lesson pages 500 on Vercel). Single deploy from repo root.
- (history) The marketing site briefly lived as a SEPARATE Next.js project at `site/` (own package.json/node_modules/.next) — deliberately NOT merged into the dashboard to avoid the `/`,`/projetos`,`/carreira` route collisions and to keep the working app untouched. Stack: Next 15 + TS + Tailwind + Framer Motion + Lucide, dark palette (#09090A bg, #8234E9 primary, neon gradient), Plus Jakarta Sans. Pages: `/ /sobre /trilhas /recursos /projetos /carreira /precos /login /cadastro`. Components in `site/components/site/`. Data mocked in `site/lib/site-data.ts`. Auth CTAs link to the platform via `NEXT_PUBLIC_APP_URL` (`.env.local`, default localhost:6464) → `${APP_URL}/auth/login|register`. Run with `cd site && npm run dev -- -p 3011` (different port than the dashboard). Builds fully static.
- **/aulas-udemy** ampliada (`components/dashboard/udemy-view.tsx`): além do CRUD, tem visão geral com gráficos (OverallRadial/StatusDonut/AreaProgressChart de `evolution-charts`), stats, "stack dos cursos" (categorias), modal de detalhe ao clicar no card, e **análise por IA** — por curso (`analyzeUdemyCourse`) e da trilha toda (`analyzeUdemyCollection`), ambas em `lib/ai/actions.ts` (OpenRouter via `chatJson`).
- **/aulas-youtube** ampliada igual (`youtube-view.tsx`): stats, gráfico de categorias, modal de detalhe e análise IA (reusa `analyzeUdemyCourse`/`analyzeUdemyCollection`; painéis compartilhados em `components/dashboard/ai-analysis.tsx` = AnalysisList/CourseAnalysisPanel/CollectionAnalysisPanel).
- **/material** virou CRUD completo: `materials` ganhou `user_id`+`file_url`+`updated_at`+RLS (`supabase/material-schema.sql`, aplicado via Management API) e bucket `material-files`. `lib/data/material-actions.ts`, `components/forms/material-form.tsx` (upload + import por URL), `components/dashboard/material-view.tsx` (filtros tipo/escopo, modal detalhe com **baixar arquivo**, editar/remover dos próprios). `Material` type ganhou `user_id`/`file_url`; novo `MaterialInput`.
- **/livros**: `livros-view.tsx` tem **modal de detalhe** (clicar no card) com **Baixar PDF** (`download` attr), Acessar, e Editar/Remover só dos próprios (`user_id===userId`). Os 7 livros seed têm user_id null (recomendados, read-only); livros criados pelo usuário são editáveis. book-covers/book-files são públicos.
- **/inicio**: seções "Como construir uma carreira em Python" (roadmap 5 etapas → conteudos/especializacoes/exercicios/meus-projetos/minha-carreira) e "Como usar a plataforma" (guia de cada rota).
- Global search (⌘K): `getSearchIndex()` (queries.ts) builds a light index (contents, stack, careers, books, materials, top 400 projects) passed from `(dashboard)/layout.tsx` → `CommandMenu` (now `index` prop, grouped+filterable; internal nav vs external links). **`CommandMenu` imports `SearchItem` with `import type` to avoid bundling server code.**
- Collapsible sidebar: `store/ui.ts` has `sidebarCollapsed`/`toggleSidebar` (localStorage `pytrack-sidebar-collapsed`). Layout now renders `components/layout/dashboard-shell.tsx` (client) which animates `DesktopSidebar` (`-translate-x-full` when collapsed) + content padding (`lg:pl-64`↔`lg:pl-0`) with `transition`. Toggle buttons: `PanelLeft` in Header + `PanelLeftClose` in sidebar header.
- `/stack` rebuilt as `stack-view.tsx` (search + category/level filters + motion) using **real tech logos**: `lib/tech-icons.ts` maps names → Devicon (jsDelivr) colored SVGs, Simple Icons fallback (celery/gunicorn/scipy/graphql/pydantic/polars/etc. use simpleicons; devicon lacks them), lucide fallback via `components/ui/tech-icon.tsx`.
- `/projetos` now **1364 projects** (`scripts/generate-projects-mega.mjs` = 106 curated + archetype×domain cross). `getProjects` paginates (>1000 cap).
- `/conteudos` expanded again to **74 modules / 339 lessons** (44 extra modules via `gen-ecosystem-content.mjs` — added Desktop/Games/CV/Pillow/TimeSeries/Geo/Web3/MicroPython/Regex/Logging/Email-PDF/Dask/MLOps/Recsys/Features/Eventos/Caching).
- `/conteudos` was ~doubled to **57 modules / 316 lessons**: `scripts/gen-ecosystem-content.mjs` writes 27 ecosystem modules as real markdown lessons under `doc/Conteudos/Ecossistema/<folder>/` + `lib/content/extra-manifest.ts` (`EXTRA_MODULES`, slugs `ext-<folder>`) + `supabase/contents_extra.sql`. `lib/content/registry.ts` now MERGES `MODULES` (base manifest) + `EXTRA_MODULES`.
- New **`/aulas-youtube`** route (nav group Recursos, Youtube icon): `youtube_content` table, per-user RLS, CRUD via `lib/data/youtube-actions.ts`; `fetchYoutubeMeta(url)` uses **YouTube oEmbed** (`/oembed?url=...`) for title/channel/thumbnail (works for videos + playlists, no scraping). UI `youtube-view.tsx` + `youtube-form.tsx`.
- `/vagas` now has user job CRUD on top of the static boards: `jobs` table + per-user RLS, `lib/data/jobs-actions.ts` with `analyzeJob({url,text})` that fetches OG (`fetchUrlMeta`) and uses **OpenRouter AI** (`chatJson`) to extract title/company/type/seniority/salary/modality/stack/skills (works even though LinkedIn blocks scraping — paste the description). `vagas-view.tsx` + `job-form.tsx`.
- "Resetar progresso" (`ResetProgressButton` in settings-actions) now clears BOTH Supabase module progress AND localStorage (lessons/exercises/questions/pomodoros) + dispatches `pytrack-progress` + `router.refresh()` (it previously only deleted Supabase rows and didn't refresh, so it looked broken).
- `/perfil` computes a Python "level" from XP (`lib/level.ts`, 6 tiers) summing modules/lessons/exercises/questions/books/courses — server `progress`/books/courses + localStorage counts. Avatar upload + bio/location/socials. `updateProfile` now takes a partial `ProfileInput` (only sets provided keys). users_profile gained bio/location/github_url/linkedin_url/website_url/xp columns.
- `/livros` is per-user CRUD (`lib/data/books-actions.ts`); books gained cover_url/file_url/user_id + owner write RLS (read stays public so seed books show as "Recomendados", only owned are editable). Cover via `fetchBookMeta` (Open Graph) or upload; book file (PDF/EPUB) upload.
- Supabase **Storage**: public buckets `avatars`/`book-covers`/`book-files` + `storage.objects` policies (`supabase/profile_books.sql`). Client uploads via `lib/storage.ts` (`uploadToBucket`). Shared OG fetcher extracted to `lib/data/meta.ts` (`fetchUrlMeta`, used by books; udemy still has its own copy).
- Home (`/`) is an interactive dashboard: `components/home/python-level-card.tsx` (reusable level card, also used on `/perfil` and `/evolucao`; reads `lib/client-progress.ts` `readLocalActivity()` for lessons/exercises/questions + server counts), `pomodoro-coffee.tsx` (coffee mug that drains during focus / refills on break, sessions in localStorage `pytrack-pomodoros`, `steam` keyframes in globals.css), `code-showcase.tsx` (Python snippets via client `CodeBlock`). Charts in `components/dashboard/evolution-charts.tsx`: `OverallRadial`, `AreaProgressChart`, `StatusDonut`. `/evolucao` reorganized to reuse these. Note: client `CodeBlock` highlights on mount so hljs classes aren't in SSR HTML (the lesson reader's server `Markdown` does highlight in SSR).
- ChunkLoadError / `Cannot find module './vendor-chunks/next.js'` / "Failed to collect page data" in dev = corrupted/stale `.next`; fix is `rm -rf .next` + restart. **The user runs their own dev server (`PORT=6464 npm run dev`) on this same folder — do NOT run `npm run build` or a second `npm run dev` while theirs is up; both write `.next` and corrupt it. Validate via typecheck or coordinate, don't spin up competing processes.** Responsive fixes: Dialog uses `w-[calc(100%-1.5rem)]`, TabsList scrolls on mobile.
- Global header level bar: `components/layout/header-level-bar.tsx` (always-visible tier emoji + XP progress bar; click opens analytical panel with XP-by-source). Fed by `getLevelCounts()` (light server counts) passed through `(dashboard)/layout.tsx` → `Header`. Updates live via `pytrack-progress` window event (dispatched from practice-exercises-view, questions-view, reader-shell) + 5s poll + focus. `/evolucao` rebuilt with `SkillRadar` (recharts radar of top-8 areas), `EcosystemAnalysis` (XP-by-source bars + activity tiles + pomodoro cups), strengths/gaps from `stats.byArea`, full ranking, next steps.
- Branding: `logo.png` (repo root, 1024²) → `public/logo.png` (sidebar + login) and `app/icon.png`/`app/apple-icon.png` (favicon, Next file-convention) via `sips -Z 256`. Sidebar/login use `next/image`.
- **AI via OpenRouter** (`lib/ai/openrouter.ts` + `lib/ai/actions.ts`, key `OPENROUTER_API_KEY` in `.env`/`.env.local`). All models are `:free` and flaky — client tries `openai/gpt-oss-120b:free` → `google/gemma-4-31b-it:free` → `nvidia/nemotron-nano-9b-v2:free`. `chatJson` requests `response_format:json_object`, parses with a string-aware brace-matching `parseJsonLoose`, and retries once (free models sometimes emit malformed JSON). `analyzeExercise` (code review: score/issues/suggestions/better_solution) is embedded in each practice card via `ai-exercise-review.tsx`. `analyzeCareer` powers the **`/consultor-ia`** route (nav group Carreira, `Bot` icon) — `components/career/career-consultant.tsx` reads server stats + localStorage activity, returns verdict (apto/quase/em_formacao)+readiness+roadmap+roles. **Dev server must be RESTARTED after env changes** (Next doesn't hot-reload `.env`).
- Exercise answer box uses a custom `components/ui/code-editor.tsx` (textarea overlaid on a highlight.js `<pre>`, transparent text so colors show through) with Python syntax colors + auto-indent (Tab/Enter/Backspace handling) — no new deps. Used in `ai-exercise-review.tsx`.
- `/projetos` has **106 projects** (`scripts/generate-projects.mjs` → `supabase/projects_expansion.json`, REST replace); projetos-view has search + area/difficulty filters + load-more + framer-motion. `/configuracoes` redesigned (sectioned: Conta/Perfil/Aparência/Plataforma/Dados/Sobre, avatar upload, platform counts, ClearLocalButton). `/carreira` uses animated `carreira-view.tsx` (area filter, market-demand/daily/seniority-salary extras curated by area). Home has framer-motion `MotionSection` entrances, `AnimatedNumber` counters, and an added SkillRadar + "Foco da semana" chart row. `StatCard.value` is now `ReactNode`.
- Practice exercises doubled to **2462** (`scripts/expand-exercises.mjs` → `supabase/expansion_exercises.json`, ex_ids `GX####`/`GV####`, appended via REST; source doc was exhausted at 1230 and its EX0567+ entries are low-quality stubs).
- `/aulas-udemy` is full CRUD per user: `udemy_courses` got `image_url`/`description`/`user_id` columns + owner RLS (`udemy_select/insert/update/delete`); seed example rows were deleted. `lib/data/udemy-actions.ts` has create/update/deleteCourse + `fetchCourseMeta(url)` which reads Open Graph; **Udemy blocks Node fetch (Cloudflare JA3 fingerprint, 403 even on http/1.1), so there's a microlink.io fallback** that returns title/image/author/description. Banner shown via plain `<img>`.
- Secrets (anon/service-role/management token) ended up in `.env.local`/README from the prompt — user was advised to rotate them.
