---
name: stripe-billing
description: "Assinatura Stripe (R$10/mês) — schema, guard, rotas, webhook (PyTrack)"
metadata: 
  node_type: memory
  type: project
  originSessionId: e80488b4-200b-45f2-a1a1-9c9739d237ee
---

PyTrack tem **assinatura mensal Stripe (R$10/mês)** gateando o dashboard. Ver [[pytrack-platform]] e [[theme-and-routes]].

**CSP + clientes fix + pentest (jun/2026)**: **CSP + security headers** em `next.config.mjs` `headers()` (só em produção p/ não quebrar HMR): CSP com `script-src 'self' 'unsafe-inline' 'unsafe-eval' cdn.jsdelivr.net` (Pyodide), connect-src supabase/jsdelivr/pypi/stripe, frame-ancestors none, object-src none + HSTS/nosniff/X-Frame-Options DENY/Referrer-Policy/Permissions-Policy. **/admin/clientes**: exclui ADMIN_EMAILS e cortesias (metadata.comp/granted_by) do faturamento (rows/MRR) e da lista (usersList filtra admins; totalUsers idem) — antes o comp do admin inflava o MRR; + busca por e-mail no ClientsDashboard. **Teste de segurança (curl)**: ✅ headers presentes, x-powered-by ausente; rotas protegidas →login; RLS bloqueia anon (0 linhas em subscriptions/support_messages/user_ai_settings/rate_limits/stripe_customers); nenhum segredo (sk/rk/service_role/sbp) no HTML; webhook 400 sem assinatura; /.env|/.git 404; INSERT anon 401.

**Uploads + consent + Dependabot (jun/2026)**: **Validação de uploads** central em `lib/upload-validation.ts` (whitelist por bucket: tamanho/extensão/MIME + `safeStoragePath` + bloqueio de svg/html/js/etc). Integrada em `lib/storage.ts` `uploadToBucket` (avatars/book-covers/book-files/material-files) e no app uploader (client `AppUploader` + server `createUploadUrl` valida ext app-releases). **Consentimento**: checkbox obrigatório (Termos+Privacidade) no AuthForm register + validação no signUp (`consent`). **Dependabot** `.github/dependabot.yml` (npm + actions semanal) + step `npm audit --audit-level=high` (continue-on-error) no ci.yml.

**DevOps/SEO/observabilidade (jun/2026)**: **CI/CD** `.github/workflows/ci.yml` (npm ci → lint → typecheck → build, node 20; script `typecheck` add no package.json). **Perf** next.config: `poweredByHeader:false`, `compress`, images AVIF/WebP, `experimental.optimizePackageImports:[lucide-react,recharts]`. **SEO**: +3 posts em lib/blog-posts.ts (python-para-automacao, machine-learning-com-python-ia, roadmap-python-2026 — total 9; sitemap 32 urls). **Monitoramento**: `lib/logger.ts` (console + LOG_WEBHOOK_URL opcional), `/api/log`, `app/global-error.tsx` (reporta erro), webhook Stripe loga via logError; `MONITORING.md` (Sentry/Logtail). **OWASP**: `SECURITY.md` (Top 10 + ASVS + rotação de secrets). **Load test**: `load-test/k6.js` + README. **Backup**: `BACKUP.md`. Secrets: `.env` gitignored, envs Vercel por ambiente; PENDENTE owner rotacionar rk_live/ghp/sbp expostos. Doc inserida via python (escape de backticks no Edit falha p/ template literals — usar script).

**SaaS hardening (jun/2026)**: `supabase/platform-extras-schema.sql` (aplicado): tabela `rate_limits` + função `rl_hit`; `community_profiles.is_blocked`; `users_profile.onboarding_goal/onboarding_done`. **Rate limiting**: `lib/rate-limit.ts` `rateLimit(key,limit,sec)` (RPC rl_hit, fail-open) aplicado em signIn (8/min), signUp (5/10min), suporte (5/min), community post (8/min)/comment (15/min). **Privacidade/Termos/Status**: páginas públicas `/privacidade`, `/termos`, `/status` (status faz ping em site/db/auth/stripe/openrouter); links no footer (Legal). **LGPD**: `/configuracoes/dados` ganhou "Excluir minha conta" (`deleteMyAccount` → admin.auth.admin.deleteUser + cascata; bloqueia admins) além do export já existente. **Onboarding**: signUp agora redireciona p/ `/onboarding` (escolha de objetivo → salva onboarding_goal → vai p/ trilha). **Moderação**: comunidade já tinha report-dialog; novo painel admin `/admin/moderacao` (denúncias pendentes → remover post/bloquear autor/resolver; lista bloqueados/desbloquear) — actions em `app/(dashboard)/admin/moderacao/actions.ts`; createPost/createComment checam is_blocked + rate limit. Nav Admin += "Moderação". **GitHub login**: botão "Entrar/Cadastrar com GitHub" no AuthForm (signInWithOAuth) + `/auth/callback` (exchangeCodeForSession) + estado de loading. **Backup**: `BACKUP.md` documentando pg_dump/PITR/Storage/config. **Config Supabase** (via Management API): manual_linking=true, site_url=www.pytrack.com.br, uri_allow_list inclui domínios + localhost (corrigiu bad_oauth_state do GitHub).

**2FA + GitHub + gestão clientes (jun/2026)**: **Gestão de clientes** em `/admin/clientes`: tabela agora lista TODOS os usuários (auth.admin.listUsers + join subs, inclui gratuitos) com coluna "Gerenciar" → `components/admin/client-row-actions.tsx` (select de plano + excluir). Actions `app/(dashboard)/admin/clientes/actions.ts`: `setUserPlan` (upsert sub cortesia por tier; price-based, metadata.granted_by; vitalicio→metadata.lifetime; free→delete) e `deleteUser` (admin.auth.admin.deleteUser, bloqueia admins). **2FA TOTP** (Supabase MFA, sem config externa): `/configuracoes/seguranca` (`components/settings/security-settings.tsx`) enroll (QR+secret)/verify/unenroll via browser client `supabase.auth.mfa.*`. Login: signIn checa `getAuthenticatorAssuranceLevel` → se aal1→aal2 redireciona p/ **`/auth/mfa`** (page client de verificação). Middleware: exenta `/auth/mfa` do redirect de auth + enforce MFA em rotas protegidas (aal1→aal2 → /auth/mfa). **GitHub**: na mesma página de segurança, `linkIdentity({provider:'github'})` / `unlinkIdentity`; exibe @username de `user.identities`. ⚠️ GitHub exige o **provider GitHub habilitado no Supabase** (OAuth App) + "Manual linking" ligado — senão o botão dá erro. **Correção de exercícios**: `ai-exercise-review` já mostra o modelo ("via X") + erro agora sugere configurar IA própria em /configuracoes/ia.

**Hero/404/BYOK-IA (jun/2026)**: hero do site corrigido (16 trilhas, 80+ módulos; antes 8/74) em `components/site/hero-section.tsx`. **`.env.example`** criado (não é gitignored — só `.env`/`.env*.local` são) com todas as envs. **404 personalizado**: `app/not-found.tsx` rico (CTAs + sugestões + link suporte). Middleware mudou: em vez de `!isPublic→login`, usa **PROTECTED_PREFIX** (lista de rotas do dashboard+assinar) → rotas DESCONHECIDAS caem no 404 (não vão p/ login); gating só roda em rotas protegidas. **BYOK IA**: usuário pode usar a própria IA (OpenRouter/OpenAI/Claude/Gemini/DeepSeek/Groq/Nvidia) — tabela `user_ai_settings` (`supabase/user-ai-settings-schema.sql`, RLS own); `chatComplete`/`chatJson` aceitam `AiOverride {apiKey,model,baseUrl}`; `lib/ai/user-settings.ts` `getUserAiOverride()`; `lib/ai/actions.ts` usa wrapper `aiJson` que injeta o override. Página `/configuracoes/ia` (form provider/base_url/model/api_key/enabled + testar conexão); padrão (OPENROUTER_API_KEY) segue funcionando se desativado.

**Rotas admin/suporte top-level (jun/2026)**: Admin/Clientes/Mensagens/Suporte SAÍRAM de /configuracoes e viraram **rotas próprias**: `/admin` (hub, create users), `/admin/clientes` (receita), `/admin/mensagens` (canal admin), `/suporte` (user→admin). Actions movidas junto (paths `@/app/(dashboard)/admin/...` e `/suporte/...`). **Nav**: novo grupo "Admin" em `lib/navigation.ts` com `adminOnly:true`; item "Suporte" no grupo Conta. Sidebar recebe `isAdmin` (threaded layout→MobileSidebar/DashboardShell→SidebarContent) e **filtra itens adminOnly + esconde grupo vazio**; badge de notif movido p/ `/admin/mensagens` (admin) ou `/suporte` (user). Páginas admin redirecionam não-admin p/ `/inicio`. `/suporte` e `/admin/*` em isFreeDashboardPath + allowlist "essential" do middleware (acessíveis mesmo com trial expirado; gating real é `isAdmin` na página). settings-nav e hub /configuracoes limpos (sem admin/suporte). Cada página movida tem PageHeader próprio.

**App mgmt + config hub + badges (jun/2026)**: **/aplicativo** detecta admin (`isAdmin`) → seção de gestão: upload de APK/binários via **signed URL** (evita limite de body da Vercel): `createUploadUrl` (server, admin) → `supabase.storage.uploadToSignedUrl` (browser, `components/admin/app-uploader.tsx`) → `recordRelease` (server). Tabela `app_releases` + bucket público `app-releases` (`supabase/app-releases-schema.sql`). Downloads usam `download_url` do release mais recente por plataforma, gated p/ Completo+ (`tierAtLeast`); sem release → "Em breve". `deleteRelease` (form action). **/configuracoes** virou hub profissional (era redirect→/conta): header do usuário (avatar/plano/membro desde) + grid de seções + seção Admin (se admin). **Badge de notificações**: layout calcula `notif` (admin=msgs não lidas dos users; user=respostas não lidas) e passa via sidebar → SidebarItem mostra badge no item Configurações; páginas suporte/mensagens marcam como lido ao abrir. **CSV**: botão "Exportar CSV" no painel de clientes (client-side Blob).

**Admin clientes + suporte (jun/2026)**: **`/configuracoes/admin/clientes`** (admin-only) = dashboard financeiro: KPIs animados (MRR/ARR/receita vitalícia/ticket/assinantes/clientes/novos30d via count-up), gráficos Recharts (MRR acumulado, novos/mês, receita Stripe/mês, pizza por plano), painel Stripe (balance + charges via `stripe.balance.retrieve`/`charges.list`) e tabela de clientes. Server page calcula (subscriptions + auth.admin.listUsers + Stripe) → client `components/admin/clients-dashboard.tsx`. Valores em `lib/billing-stats.ts` (PRICE_MONTHLY/PRICE_ONETIME/fmtBRL/lastMonths). **Suporte**: tabela `support_messages` (`supabase/support-schema.sql`, RLS: user vê/insere próprio thread sender='user'; admin via service role). Usuário: **`/configuracoes/suporte`** (form + thread) — `sendSupportMessage`. Admin: **`/configuracoes/admin/mensagens`** (inbox agrupado por user + responder) — `replyToUser`. settings-nav: "Suporte" p/ todos; "Clientes & receita" + "Mensagens" só admin. Hub /configuracoes/admin tem atalhos.

**Free 7 dias + comparativo + home (jun/2026)**: modelo do plano **Grátis mudou**: agora é **7 dias de acesso** (não permanente) — middleware: usuário sem assinatura paga tem `(now - user.created_at) <= 7 dias` p/ acessar rotas grátis; após 7 dias → bloqueia tudo (redirect `/assinar?trial=expired`), exceto `/assinar` e `/configuracoes/*`. `/assinar` mostra aviso de trial expirado. **Comunidade voltou p/ Completo** (re-adicionada em COMPLETO_ONLY — o usuário reverteu o pedido anterior de Essencial). **/precos**: coluna **Vitalício** no comparativo (agora 6 colunas, scroll-x, min-w-680, com linhas "pagamento único" e "atualizações futuras"); card Grátis = "7 dias". **Home** (`app/(site)/page.tsx`): seção de trilhas usa `TRILHAS` (16, não mais TRACKS) + link "ver todas"; seção de preços trocada de PricingCard único p/ grid dos 4 planos + banner Vitalício. **Comunidade limpa**: deletados todos os posts/interações (community_posts/likes/comments/shares/saved/reports/notifications) + vagas (community_jobs + jobs); perfis mantidos.

**Vitalício + reembolso + downgrade + favicon (jun/2026)**: **Plano Vitalício** = pagamento único R$697 (`price_1TfV9dCB6Dz1wPeibUtTjFbZ`, prod `prod_Ueon0LzOceUKo0`, env `STRIPE_PRICE_ID_VITALICIO`). Tier `vitalicio` (rank 4, topo) em billing-access; `tierOf`: metadata.lifetime→vitalicio. checkout: `isLifetimePlan('vitalicio')` → mode `payment` (one-time); webhook trata `checkout.session.completed` mode=payment → upsert sub metadata.lifetime=true (period 2126). UI: card Vitalício no PlanSelector + banner no /precos. **Reembolso** (garantia 7 dias, autoatendimento): `app/(dashboard)/configuracoes/plano/actions.ts` `requestRefund` acha payment_intent (lifetime metadata ou latest_invoice da sub), valida janela 7 dias, `refunds.create` + cancela sub + **deleta** a row local (revoga acesso). `components/billing/RefundButton`. /precos destaca "Garantia de 7 dias". **Downgrade**: /configuracoes/plano lista tiers abaixo do atual (só recorrentes) c/ SubscribeButton → subscriptions.update proration. **Favicon**: `app/icon.png` + `app/apple-icon.png` do new-logo (redimensionado via sips 256/180). README ampliado p/ 1013 linhas (classificado como Micro-SaaS vertical EdTech).

**Admin + sidebar locks (jun/2026)**: `lib/admin.ts` `ADMIN_EMAILS=[contato@estevamsouza.com.br, estevamsouzalaureth@gmail.com]` + `isAdmin(email)`. Admin = Suprema **vitalício** via subscription `metadata.comp=true` (tierOf→suprema, period_end 2126). contato@ já existe (id ea839ab8-aaae-41fd-893f-016185469ee0); senha definida via Auth admin API (service role) + comp sub. Painel **/configuracoes/admin** (admin-only, redirect senão; link no settings-nav só p/ admin via prop isAdmin) cria usuários Suprema vitalícios: server action `createSupremaUser` (`admin.auth.admin.createUser` email_confirm + upsert comp sub). **Sidebar locks**: `requiredTierForPath()` em billing-access; layout passa `tier`(getUserTier) → MobileSidebar/DashboardShell → SidebarContent → SidebarItem mostra cadeado em rota acima do plano e linka p/ `/assinar?upgrade=<tier>`. **Comunidade movida p/ Essencial** (saiu de COMPLETO_ONLY).

**Fail-open por design**: `billingEnabled = STRIPE_SECRET_KEY && STRIPE_PRICE_ID` (`lib/stripe/server.ts`). Se billing NÃO está configurado, `userHasAccess()` retorna true → dashboard funciona normal. Só ativa o bloqueio quando o owner configurar as env vars + price + webhook. ⚠️ As chaves live coladas no chat (pk_live / rk_live) foram pedidas para ROTACIONAR (comprometidas).

**Banco** (`supabase/stripe-subscriptions-schema.sql`, aplicado via Management API): tabelas `stripe_customers`, `subscriptions` (unique por user_id), `payment_events` (dedupe de webhook). RLS: usuário lê só o seu; escrita só via service role (webhook). 

**Acesso** (`lib/stripe/subscriptions.ts`): `hasDashboardAccess` = status active/trialing OU current_period_end > now (bloqueia unpaid/incomplete_expired). Guard no `app/(dashboard)/layout.tsx`: `if (!await userHasAccess(user.id)) redirect('/assinar')`. Helpers em `lib/auth/{get-current-user,require-subscription,subscription-guard}.ts`. Admin client em `lib/supabase/admin.ts` (aceita SUPABASE_SERVICE_ROLE_KEY ou SUPABASE_SERVICE_ROLE).

**Rotas**: `app/assinar/page.tsx` (página premium, precisa login), `app/(dashboard)/configuracoes/plano/page.tsx` (+ item "Plano e cobrança" na settings-nav). APIs (runtime nodejs): `create-checkout-session` (mode subscription, metadata user_id, success→/inicio?checkout=success, cancel→/assinar), `create-portal-session` (billing portal, return→/configuracoes/plano), `webhook` (raw body, constructEvent, dedupe via payment_events, trata checkout.session.completed / customer.subscription.* / invoice.payment_* / customer.updated). Middleware: `/api` adicionado a PUBLIC_PREFIX (rotas API se autenticam sozinhas; webhook é público). signUp redireciona p/ `/assinar`.

**Componentes**: `components/billing/{SubscribeButton,ManageSubscriptionButton,SubscriptionStatusCard,PricingCard}.tsx`.

**FIX loop /assinar (jun/2026)**: o gating do middleware redirecionava não-assinantes p/ `/assinar`, mas `/assinar` não estava isento → ERR_TOO_MANY_REDIRECTS. Corrigido adicionando `/assinar` ao `isFreeDashboardPath` (billing-access.ts). Qualquer página alvo de redirect do gating DEVE estar isenta. (Build local com `.next` stale dá "Cannot find module for page" — sempre `rm -rf .next` antes; a Vercel builda limpo.)

**Go-live domínio + fix planos (jun/2026)**: domínio **www.pytrack.com.br LIVE** (apex pytrack.com.br faz 308→www; www é canônico). `NEXT_PUBLIC_APP_URL=https://www.pytrack.com.br` (Vercel+.env) e webhook `we_1TfPB2…` apontado p/ `https://www.pytrack.com.br/api/stripe/webhook` (mesmo secret). Bugs corrigidos: (a) `/configuracoes/conta` mostrava "Plano Gratuito" **hardcoded** → agora usa `getUserTier()` + `TIER_LABEL`; (b) `/configuracoes/plano` mostra o nome real do plano (SubscriptionStatusCard recebe `tier`) + card "Fazer upgrade para Completo" quando tier=essencial; (c) `/assinar` PlanSelector recebe `currentTier` e marca "Seu plano atual"/"Incluído no Completo"/"Fazer upgrade"; (d) **upgrade agora funciona**: `create-checkout-session` detecta assinatura ativa e faz `stripe.subscriptions.update` com proration (em vez de novo checkout duplicado) → redireciona p/ `/configuracoes/plano?upgraded=1`. Novo helper `getUserTier(userId)` em subscriptions.ts; `TIER_LABEL` em billing-access.

**Tiers + domínio (jun/2026)**: 2 planos pagos. **Essencial** = R$10/mês (`price_1TfPAn…`) / R$96/ano (`price_1TfQ0A…`) → trilhas/exercícios/IDE/evolução/materiais. **Completo** = R$19/mês (`price_1TfQedCB6Dz1wPeiXWheSFrk`) / R$182/ano (`price_1TfQeeCB6Dz1wPeiv5tsuwdx`, produto `prod_Uek8rBW1CNxTkb`) → tudo (comunidade/projetos/especializações/consultor/vagas/carreira). Env: `STRIPE_PRICE_ID_COMPLETO(_ANNUAL)`. `lib/billing-access.ts`: `TIER_BY_PRICE`, `tierOf(sub)` (comp metadata = completo), `requiresCompleto(pathname)` (comunidade/meus-projetos/especializacoes/consultor-ia/minha-carreira/vagas/perguntas). Middleware gating tier-aware → rota Completo p/ user Essencial = redirect `/assinar?upgrade=completo`. checkout aceita `plan` ∈ {essencial_monthly,essencial_annual,completo_monthly,completo_annual} → `priceForPlan()`. UI: `components/billing/PlanSelector.tsx` (2 cards + toggle) no `/assinar`; `/precos` público com 3 colunas (Grátis/Essencial/Completo) + comparativo. **Domínio pytrack.com.br** adicionado ao projeto Vercel (apex+www); DNS no registro.br: `A @ 76.76.21.21` e `A www 76.76.21.21` (ou nameservers ns1/ns2.vercel-dns.com). **PENDENTE go-live** (quando DNS propagar): trocar `NEXT_PUBLIC_APP_URL`→https://pytrack.com.br e o webhook Stripe (`we_1TfPB2…`) → https://pytrack.com.br/api/stripe/webhook (mesmo secret) + redeploy. Por ora mantidos em vercel.app p/ não quebrar no período de propagação. **SEO**: JSON-LD (Organization+WebSite) no root layout; GSC file já no ar.

**Freemium + SEO (jun/2026)**: gating de assinatura agora é no **middleware** (`lib/supabase/middleware.ts`), path-aware, lendo a sub via anon client + RLS (não no layout). Lógica pura em `lib/billing-access.ts` (`hasDashboardAccess`, `isFreeDashboardPath`, `FREE_MODULE_SLUG="parte-1-basico"`). **Rotas grátis** (logado, sem assinatura): `/inicio`, `/ide`, `/conteudos` + 1º módulo `parte-1-basico`, `/perfil`, `/configuracoes/*`; o resto exige plano → redirect `/assinar`. Layout do dashboard mostra `<UpgradeBanner/>` se `!userHasAccess`. signUp agora redireciona p/ `/inicio` (funnel freemium). **Conteúdo público p/ SEO**: `/aprender` + `/aprender/[licao]` (site group, lições do 1º módulo via `lib/content/registry`, indexável, no sitemap; outputFileTracingIncludes cobre `/aprender`). **GSC**: arquivo `public/googlea21980fd6a1f1139.html` (método HTML file) — liberado no middleware via prefixo `/google`. Verificações: preços confirmados R$10/mês + R$96/ano ativos.

**Crescimento (jun/2026)**: (1) **Trial 7 dias** — `STRIPE_TRIAL_DAYS=7`; checkout aplica `trial_period_days` só na 1ª assinatura do user. (2) **Plano anual** — price `price_1TfQ0ACB6Dz1wPei7hdTfaQS` (R$96/ano), env `STRIPE_PRICE_ID_ANNUAL`; checkout aceita `{plan:'monthly'|'annual'}` no body; `PricingCard` é client com toggle. (3) **SEO** — `app/sitemap.ts`, `app/robots.ts` (bloqueia rotas privadas), `app/opengraph-image.tsx`+`twitter-image.tsx` (next/og branded), metadata rica no root layout. (4) **Blog público** — `lib/blog-posts.ts` (4 artigos md) + `app/(site)/blog` e `/blog/[slug]` (react-markdown, `.markdown` class); link no footer. (5) **Indicação** — `supabase/referrals-schema.sql` (`referrals` + `community_profiles.referral_code`), cupom `INDICACAO1MES`/env `STRIPE_REFERRAL_COUPON` (mas a recompensa real é **crédito de saldo R$10** via `customers.createBalanceTransaction` — `customers.update({coupon})` foi removido do SDK v22); signUp captura `?ref=` (form hidden via register page), webhook recompensa o indicador quando o indicado fica active/trialing; página `/configuracoes/indicacoes`. **CRÍTICO**: middleware PUBLIC_PREFIX precisa incluir `/blog`, `/sitemap`, `/robots`, `/opengraph-image`, `/twitter-image` senão o middleware redireciona crawlers p/ login.

**Gotcha resolvido**: o `createAdminClient` (guard + webhook) precisa de `SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_SERVICE_ROLE` **na Vercel** — faltava, então `/inicio` quebrava (Server Components render error) e o webhook falhava ao gravar. Adicionado nas envs da Vercel. Além disso, `userHasAccess`/`getUserSubscription` agora têm try/catch **fail-open** (nunca derruba o dashboard; em erro libera acesso). Quem pagou antes do fix (estevams186@gmail.com, sub_1TfPJf…) foi sincronizado manualmente; webhooks futuros gravam sozinhos (Stripe também re-tenta os que falharam).

**JÁ ATIVO (configurado via API em jun/2026)**: produto `prod_Ueick1tmTL37It`, price **`price_1TfPAnCB6Dz1wPei2nOAIofK`** (R$10/mês BRL), webhook `we_1TfPB2CB6Dz1wPei7mbNqSRG` (7 eventos) → /api/stripe/webhook. Env vars setadas na Vercel (production/preview/development) com a restricted key `rk_live_` como STRIPE_SECRET_KEY (tem perms de products/prices/webhooks/customers/checkout/portal). O **owner** (estevamsouzalaureth@gmail.com, user_id fa31bc0c-9abd-47f1-bf0f-9a619d589092) tem uma **assinatura de cortesia** (status active, period_end ~2126, metadata comp:true) p/ não ser trancado — pode deletar a linha em `subscriptions` p/ testar o fluxo real. ⚠️ A rk_live_ apareceu no chat → recomendado regenerar a restricted key no Stripe e atualizar STRIPE_SECRET_KEY na Vercel. Deps: stripe ^22, @stripe/stripe-js.
