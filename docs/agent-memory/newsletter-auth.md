---
name: newsletter-auth
description: "Newsletter (popup+admin), templates email Resend profissionais, login/registro redesign orbiting tech (jun/2026)"
metadata: 
  node_type: memory
  type: project
  originSessionId: e80488b4-200b-45f2-a1a1-9c9739d237ee
---

**NPS + Utmify (11/jun/2026 madrugada):**
- **NPS/RECOMENDAÇÃO**: tabela `nps_responses`(score 0-10/reason/email, RLS insert true). `lib/nps-actions.ts` submitNps. `components/dashboard/nps-survey.tsx` (2 passos: nota 0-10 com "pouco/muito provável" + textarea motivo, card escuro #1e2435 bottom-right, localStorage pytrack-nps-seen 25s delay, Pular/Seguinte estilo recomendacao.png). Montado no (dashboard)/layout. Rota `/admin/recomendacoes` (NPS=(promotores9-10 − detratores0-6)/total×100, média, lista) + nav (ThumbsUp).
- **UTMIFY** (faturamento aparece no painel Utmify): token UTMIFY_API_TOKEN=<UTMIFY_API_TOKEN_ROTACIONAR> no Vercel+.env. Script `docs/utmify-utms-script.txt` (cdn.utmify.com.br data-utmify-is-stripe) adicionado em app/layout.tsx via next/script. `lib/utmify.ts` sendUtmifyOrder→POST api.utmify.com.br/api-credentials/orders (header x-api-token); ⚠️ customer.ip OBRIGATÓRIO (default 0.0.0.0), createdAt "YYYY-MM-DD HH:mm:ss" UTC, platform "PyTrack". Webhook checkout.session.completed envia order paga (amount_total, customer_details, UTMs+IP de metadata). create-checkout-session captura utm cookies/body + x-forwarded-for IP → utmMeta na session metadata. CSP liberou cdn/api.utmify.com.br. Testado API: SUCCESS 200. main f5135dc + tag v1.2.3. **+v1.2.4**: reembolsos webhook charge.refunded→Utmify status refunded/chargedback (orderId=payment_intent p/ casar c/ venda) + gráfico NPS por mês em /admin/recomendacoes. **+v1.2.5**: renovações invoice.payment_succeeded billing_reason=subscription_cycle→Utmify (1º pgto via checkout evita duplicar); utmMeta salvo em subscription_data.metadata. ⚠️ CONTAS DE ANÚNCIO GOOGLE na Utmify: NÃO dá p/ criar/conectar via API (token é order-only); precisa OAuth no painel Utmify (Integrações→Google Ads) + conta Google Ads do usuário — só orientei.

**CLI fix + SEO/taxas/novidades + currículo (11/jun/2026 noite):**
- **FIX CLI SaaS**: public/pytrack-saas.sh lia `read` do PIPE (curl|bash) → credenciais vazias → falhava. Corrigido: `read < /dev/tty` (TTY=/dev/tty, checa -r). + scaffold nextjs-supabase MUITO mais completo (landing/precos/login/registro/dashboard/checkout/webhook/middleware/supabase ssr/RLS schema).
- **/admin/seo**: GooglePreview usava título/desc do fetch da home (falhava) → agora usa seo.title/seo.description salvos (getSeoSettings) + line-clamp.
- **/admin/taxas**: +provedores Resend/Google(Analytics/Ads/Console)/Sentry com ícones; +card custo infra (Supabase/Vercel/Resend/Sentry Pro = ~US$98/mês) + card impostos (anualiza stripeGross×12 → MEI<81k senão Simples ~6%, contador, NF).
- **/novidades**: news-data.ts +10 entradas jun/10-11 (chat IA, ofertas, CLI, feedback, estudo/streak, efeitos, newsletter/LGPD, painéis admin) + ROADMAP atualizado (removidos certificados/extensão/apps já feitos).
- **/curriculo**: lib/resume/types.ts +9 templates (neon/sunset/forest/royal/mono/ocean/coral/slate/gradient = 22 total) + campo `photo` em ResumeData/EMPTY. render.ts renderiza foto (sidebar 96px + header-band 88px flex). Tabela resumes +is_public (RLS public select). Rota pública **/cv/[id]** (admin client, extractBody do renderResume, OG tags). actions.ts +publishResume(id,bool). Editor: input foto (URL+preview) + botões LinkedIn (share-offsite) / Link público (copia /cv/id). main ceeaea4 + tag v1.2.2.

**Sentry + email template + ofertas + CLI SaaS (11/jun/2026):**
- **SENTRY ATIVO**: org `pytrack`, projeto **pytrack-web** criado via API (token sntryu_d23058... tem project:write; 1º token sntryu_57c21... só project:read não criava). DSN=https://<SENTRY_DSN> em SENTRY_DSN+NEXT_PUBLIC_SENTRY_DSN (Vercel prod/preview/dev + .env). Config em instrumentation*.ts + sentry.server.config.ts (env-gated).
- **TEMPLATE E-MAIL**: template-email.html (profissional dark/gradiente/benefit cards) → 3 URLs fixas → {{ .ConfirmationURL }} → aplicado em mailer_templates_confirmation_content (Management API config/auth 200 + SMTP Resend).
- **OFERTAS**: tabela `offers` (RLS select active=true). `lib/offers-actions.ts` createOffer→stripe.coupons.create+promotionCodes.create (cast as never), só 1 ativa. `components/admin/offers-manager.tsx` (4 presets). `/admin/ofertas`+nav(Tag). Banner countdown `components/site/offer-banner.tsx` no site/layout. Checkout auto-aplica discounts[] (remove allow_promotion_codes quando há oferta).
- **CLI SCAFFOLD** (/construir-saas): `lib/saas-scaffold.ts` generateScaffold→bash base64 por stack (nextjs-supabase/fastapi/flask/django/express). `/api/saas/scaffold` POST{email,password,stack,name}→signInWithPassword→getUserTier→**tierAtLeast completo** (bloqueia free+essencial). `public/pytrack-saas.sh` (curl|bash). Card `components/saas/saas-cli-card.tsx`. ⚠️ /construir-saas é Suprema-gated na rota mas CLI endpoint Completo+ (boundary no endpoint).
- **COMMIT**: main ea63044 + tag v1.1.6. ⚠️ api.github.com REST 000 deste ambiente — releases v1.1.3-v1.1.6 são só TAGS no GitHub (git push funciona; criar release pelo tag depois). getUserTier de lib/stripe/subscriptions; tierAtLeast de lib/billing-access.

**Newsletter + email templates + auth redesign (jun/2026):**

- **EMAIL TEMPLATES**: `lib/email-templates.ts` shell() melhorado (header gradiente roxo, links sociais GitHub/LinkedIn/Telegram, footer 2 colunas, footerNote opcional). +`newsletterWelcomeTemplate()`, +`campaignEmailTemplate(subject,body)` (parágrafos por \n\n). **Template de confirmação do Supabase RE-APLICADO** com o novo design (gerado via `npx tsx /tmp/gen-tpl.ts` importando confirmEmailTemplate("{{ .ConfirmationURL }}"), 3249 chars com gradiente) + SMTP Resend junto no mesmo PATCH config/auth (Management API exige). ⚠️ Resend template editor visual (URL no painel) NÃO editável pela API send-only — emails usam templates do código/Supabase, não o editor visual do Resend.
- **NEWSLETTER**: tabelas `newsletter_subscribers` (email unique, status, source, RLS: insert policy true=qualquer um inscreve, SEM select policy=anon não lê emails alheios) + `newsletter_campaigns`. `lib/newsletter-actions.ts` subscribeNewsletter (upsert admin client + email boas-vindas Resend) + unsubscribe. **Popup** `components/site/newsletter-popup.tsx` (1ª visita após 3.5s, localStorage pytrack-newsletter-seen, centralizado, header gradiente, form email). Montado no app/(site)/layout.tsx. **Admin** `lib/admin-newsletter.ts` (getNewsletterData, sendCampaign envia a todos ativos via Resend em loop + grava newsletter_campaigns, removeSubscriber, ensureAdmin). `components/admin/newsletter-manager.tsx` (stats + composer assunto/corpo + histórico + lista inscritos). Rota `/admin/newsletter` (app/(dashboard)/admin/newsletter). Nav admin +Newsletter (Mail icon). Verificado: anon INSERT 201, anon SELECT [].
- **LOGIN/REGISTRO redesign** (prompts/login.md design): auth/layout.tsx já tinha split-screen; ADICIONADO `components/auth/orbiting-tech.tsx` (ícones lucide do ecossistema Python orbitando o logo PyTrack, 2 anéis CSS, anéis decorativos). Keyframes em globals.css: orbit-spin/orbit-spin-rev + .orbit-ring/.orbit-ring-rev (ring rotaciona, ícone counter-rotaciona p/ ficar reto). AuthForm em components/forms/auth-form.tsx (mode login/register, server actions, referralCode). prompts/login.md tem componente completo (AnimatedForm/TechOrbitDisplay/AuthTabs, usa motion/react→framer-motion v11 instalado) — adaptei o conceito orbiting.
- **RELEASES**: deploy → release GitHub estevam5s/pytrack incrementando versão. v1.1.0 (features), v1.1.1 (newsletter+auth), v1.1.2 (depoimentos+visitantes+fix sidebar). Próxima: v1.1.3.

**MAPA-MÚNDI visitantes (jun/2026):** `components/admin/world-map.tsx` (client, choropleth). Stack: **d3-geo + topojson-client** (NÃO react-simple-maps — conflita com React 19 ERESOLVE). topojson world-atlas baixado em `public/world-110m.json` (108K, countries-110m), fetch client-side → feature() → geoNaturalEarth1().fitSize → geoPath → SVG paths coloridos por hsl(265 70% L%) conforme volume, tooltip hover, legenda. `lib/iso-numeric.ts` ALPHA2_TO_NUMERIC (~150 países, alpha-2→ISO numérico 3díg p/ casar com id do topojson). Integrado em /admin/visitantes (WorldMap data=countries). ⚠️ topojson id = numérico string ("076"=Brasil). Build verde, /world-110m.json 200. ⚠️ release v1.1.3 GitHub PENDENTE (api.github.com inacessível no deploy — HTTP 000; verificar/criar depois).

**Depoimentos + Visitantes + fix sidebar XP (jun/2026):**
- **DEPOIMENTOS redesign** (prompts/testemunhas.md): `components/site/testimonials-marquee.tsx` (3 colunas framer-motion scroll vertical infinito y:0→-50% loop, mask-image fade top/bottom, durations 24/30/27s, cards com Quote+texto+avatar gradiente). TESTIMONIALS expandido 3→9 em lib/site-data.ts. app/(site)/page.tsx usa TestimonialsMarquee (era grid estático).
- **FIX SIDEBAR XP/nível**: card de perfil mostrava users_profile.xp (=0) mas header usa XP COMPUTADO (computeXp(serverCounts+localStorage)/levelFromXp em lib/level.ts, ~241 XP). Sidebar.tsx agora client computa igual: readLocalActivity()+computeXp+levelFromXp, mostra level.tier.emoji/name + progressToNext. SidebarProfile interface: name/avatar/streak/**serverCounts**{modules,books,courses} (era xp/level). Layout passa levelCounts em serverCounts.
- **ADMIN VISITANTES (geo mundial)**: tabela `site_visits` (country/city/region/path/referrer/session_id/user_agent, RLS sem policies=anon bloqueado, só service role). Endpoint `app/api/track-visit/route.ts` (lê x-vercel-ip-country/city/country-region headers Vercel → insert admin client). `components/site/visit-tracker.tsx` (client, 1x/sessão via sessionStorage, fetch keepalive, montado no site/layout). `lib/admin-visits.ts` getVisitAnalytics (total/today/week/uniqueVisitors/countries com countryInfo()=Intl.DisplayNames+emoji flag/topPaths/last14/recent) + countryInfo(code). Página `/admin/visitantes` (stats + gráfico 14 dias + barras por país + top páginas + recentes). Nav admin +Visitantes (Globe). Verificado: POST→200 registra BR/Florianópolis, anon SELECT→[].
- **CANCELAMENTO/REEMBOLSO JÁ FUNCIONAVA**: `components/billing/RefundButton.tsx` form (reason select + improvement + details) → `feedback-actions.ts` saveCancellationFeedback (salva cancellation_feedback + sendEmail ao admin via to default CONTACT_TO/GMAIL_USER). action enum refund|cancel|downgrade. NÃO precisou criar.