---
name: ui-improvements
description: "Melhorias UI jun/2026: fix notif DELETE policy, certificado PDF/OG/download, +5 trilhas, vídeo painel home, sidebar redesign"
metadata: 
  node_type: memory
  type: project
  originSessionId: e80488b4-200b-45f2-a1a1-9c9739d237ee
---

**Chat IA + feedback + fixes (11/jun/2026):**
- **CHAT IA `/chat`** (login+Suprema): tabelas `ai_conversations`(title/updated_at)+`ai_messages`(role/content) RLS own — ⚠️ chat_conversations/chat_messages JÁ EXISTEM (suporte visitor/admin), por isso usei ai_*. `lib/ai-chat-actions.ts` (gate Suprema, list/get/create/delete/rename/sendChatMessage usa chatComplete de lib/ai/openrouter + PLATFORM_KNOWLEDGE). `components/chat/ai-chat-interface.tsx` (sidebar conversas+histórico+excluir+datas, mensagens Markdown, composer estilo chat.md #1a1a1e, sugestões). `app/chat/page.tsx` (top-level, gate suprema senão upsell). Botão Maximize2 no chat-widget header abre /chat nova aba.
- **FEEDBACK**: tabela `site_feedback` (RLS insert true). `lib/feedback-actions.ts` submitFeedback (insert+email admin). `components/site/feedback-button.tsx` (flutuante bottom-24 right-6 ACIMA do chat, estrelas+categoria+msg). Montado no site/layout.
- **FIX TUTORIAL** (onboarding-tour.tsx): tirou router.push do goTo (navegar remontava→travava no passo 0) + guarda localStorage DONE_KEY "pytrack-tutorial-done" (não reaparece). useEffect abre só se initialOpen && !localStorage.
- **PLANO ANUAL**: já funcionava no checkout (priceForPlan _annual) + webhook (TIER_BY_PRICE anuais). Faltava home levar ciclo: PricingPlans CTA →/assinar?ciclo=anual quando anual; assinar/page lê ciclo→PlanSelector defaultAnnual.
- **NOTIF COMUNIDADE**: `components/rede/community-notification-bell.tsx` (dropdown community_notifications: message/read/type/post_id, abas, mark read) inserido no rede-topnav antes do "Eu", removido item Avisos duplicado do nav desktop.
- **CACHE ROTAS**: staleTimes 300/600. main 157f596 + tag v1.2.1.

**Router cache + central notificações (11/jun/2026):**
- **CACHE DE ROTAS**: next.config.mjs experimental.staleTimes { dynamic: 180, static: 300 } — Router Cache do Next reusa rotas já visitadas (voltar carrega instantâneo, sem refetch). 
- **CENTRAL DE NOTIFICAÇÕES** (design prompts/notificacao.md): `components/layout/notification-bell.tsx` reescrito — abas Todas/Não lidas com badges de contagem, ícones por tipo (typeIcon: message/like/follow/badge/broadcast/info), timeAgo, marcar individual (markOne ao clicar) + todas (markAll), unread dot + bg destaque, empty state BellOff, rodapé "Ver todas" →/notificacoes, header com CheckCheck + Settings. Mantém realtime INSERT + som. Badge anim-pulse-glow. Sino no dashboard header (header.tsx NotificationBell initialCount). main 7c48bc5 + tag v1.2.0.

**Pricing animado + animações site (11/jun/2026):**
- **PRICING HOME** (design prompts/price.md): `components/site/pricing-plans.tsx` (client framer-motion) substituiu grid estática na home (page.tsx seção Planos). Toggle mensal/anual layoutId "freq-pill", preço anima, badge -20% anual, TODAS features por plano (Grátis/Essencial/Completo destaque/Suprema), BorderBeam (offsetPath) no Completo, card vitalício R$697. Anual: Essencial 96/Completo 182/Suprema 442.
- **ANIMAÇÕES TODAS AS ROTAS**: `app/(site)/template.tsx` anima entrada fade+slide TODA rota. globals.css .text-gradient animado (gradient-pan 6s, todos os títulos via GradientText) + keyframes float-soft/fade-up/pulse-glow + .anim-* + hover .card translateY(-4px). @media prefers-reduced-motion desativa tudo. NÃO clonei skill externa github (ui-ux-pro-max) — apliquei princípios direto.
- **COMMIT**: main 77fdbfe + tag v1.1.8.
- **EFEITOS FUTURISTAS PREMIUM (v1.1.9, all rotas site)**: `components/site/ambient-background.tsx` (fixed -z-10 no site/layout, montado antes dos banners): base bg-background + 3 auroras CSS animadas (aurora-1/2/3 keyframes aurora-move, blur 90px, roxo/magenta/azul) + grid mascarado radial + brilho 600px que SEGUE O MOUSE (pointermove rAF, só desktop) + vinheta. `components/site/scroll-progress.tsx` (framer useScroll+useSpring, barra topo z-60 gradiente origin-left). Hero `bg-[#0a0a0f]`→transparente p/ aurora aparecer (grid opacity 40→30). globals.css: .aurora*, .gradient-border (borda gradiente animada mask-composite), .glow-hover (shadow+translateY). Tudo em @media prefers-reduced-motion: reduce desativado. Body é opaco mas -z-10 fixed pinta acima + seções container transparentes = auroras visíveis. main c8cad46 + tag v1.1.9.

**Melhorias UI/UX (jun/2026):**

- **FIX notificações**: faltava policy de DELETE em `notifications` E `community_notifications` (só tinham SELECT+UPDATE) → excluir/limpar falhava silenciosamente e "voltava ao atualizar". Adicionadas `notif_delete_own` + `cnotif_delete_own` (delete using auth.uid()=user_id) via Management API. Dashboard /notificacoes (notifications-list.tsx) já tinha botões delete/clear/filtros. ⚠️ rede /comunidade/notificacoes ainda SEM delete/clear (não feito).
- **CERTIFICADO**: download PNG + OG preview social + print/PDF. `lib/certificate-image.tsx` (certImageElement JSX p/ next/og). `app/(site)/certificado/[code]/opengraph-image.tsx` (ImageResponse 1200x630 = preview social automático). `app/api/certificate/[code]/image/route.ts` (PNG download). page generateMetadata: openGraph+twitter images=/api/certificate/[code]/image (compartilhar nas redes mostra o certificado). `components/certificate/cert-download.tsx` (Baixar PNG + Imprimir/PDF window.print). Adicionado na página verificação + modal certificate-button. Verificado: img 200 image/png, og:image na meta.
- **+5 TRILHAS** (lib/trilhas.ts, agora 22): visao-computacional(completo/ScanEye), nlp-llms(completo/MessagesSquare), game-dev(essencial/Gamepad2), sre-confiabilidade(suprema/Activity), backend-distribuido(completo/Network). Cada uma ~14-16 topics densos. Inseridas antes da suprema.
- **VÍDEO painel home**: .mov 95s convertido via ffmpeg → public/dashboard-preview.mp4 (4MB, h264 scale 1280 crf30) + .webm + .jpg poster (GIF de 95s daria 30MB, MP4 muito melhor). `components/site/scroll-video.tsx` (IntersectionObserver autoplay no scroll threshold 0.35, muted loop playsInline, pausa ao sair — comporta como GIF). dashboard-showcase.tsx troca Image /dashboard.png por ScrollVideo. ⚠️ ffmpeg em /opt/homebrew/bin.
- **SIDEBAR redesign** (estilo ViraDev ref, mantendo rotas PyTrack): sidebar-item.tsx item ativo agora = caixa border-primary/40 + bg gradiente + ChevronRight (era left-bar). **Card de perfil no rodapé** (SidebarProfile prop: name/avatar/level/xp/streak): avatar+nome+nível, barra XP (meta = próximo múltiplo 500), streak 🔥Xd → link /perfil. Threading: layout fetch users_profile(xp,current_level)+exercise_completions(streak dias consecutivos) → sidebarProfile → DashboardShell+MobileSidebar → DesktopSidebar/SidebarContent. SidebarProfile interface exportada de sidebar.tsx.

**Observabilidade + alertas webhook + LGPD (jun/2026):**
- **SENTRY (env-gated)**: @sentry/nextjs instalado (OK c/ React 19). sentry.server.config.ts + instrumentation-client.ts + instrumentation.ts (register/onRequestError) — só inicializa se SENTRY_DSN/NEXT_PUBLIC_SENTRY_DSN setado (no-op sem). Sem withSentryConfig (evita source maps). User precisa add DSN p/ ativar.
- **OBSERVABILIDADE self-hosted (funciona já)**: tabela `error_logs`. `lib/logger.ts` logEvent APRIMORADO: além de console+LOG_WEBHOOK_URL, persiste erros/warns em error_logs (admin client) + Sentry.captureException se DSN. /api/log (global-error.tsx reporta) já usa logEvent → erros de cliente persistem. ⚠️ lib/log-error.ts criado e REMOVIDO (duplicava logger existente — usar lib/logger.ts logError(context,error,meta)).
- **ALERTA WEBHOOK**: app/api/stripe/webhook catch → logError + sendEmail ao admin ("🚨 Falha no webhook Stripe"). 
- **/admin/saude +erros recentes**: getPlatformHealth retorna errors{last24h, recent[8]}, seção na página + insight se >10 erros/24h.
- **LGPD**: docs/LGPD-DPA-Retencao.md (DPO Estevam, privacidade@pytrack.com.br, papéis controlador/operadores Supabase/Vercel/Stripe/Resend/IA, DPA termos, política retenção por tipo de dado, direitos art.18, bases legais). Página /privacidade reforçada: seção 8 Retenção (tabela), 10 Operadores, 11 Contato DPO (privacidade@pytrack.com.br, 15 dias).
- **COMMIT**: main cd86b2a + tag v1.1.5 pushed. ⚠️ api.github.com REST ainda 000 (releases formais pendentes; tags v1.1.3/4/5 no GitHub).

**Estudo+anotações+saúde+docs (jun/2026):**
- **Tempo de estudo + ANTI-TRAPAÇA**: tabelas `study_sessions`(slug/title/area/seconds), `daily_activity`(user_id+day PK/seconds/lessons), `module_notes`(unique user+slug/body). `lib/study-actions.ts` (recordStudyTime insert+upsert daily, saveNote/getNote, getStreakInfo). ReaderShell (components/content/reader-shell.tsx) agora: timer client (1s/s visível, flush 30s recordStudyTime, beforeunload), botões "Marcar lida"/"Próxima"/"Finalizar" **DISABLED até MIN_STUDY_SECONDS=45s** (não pode passar rápido), painel lateral de anotações (auto-save debounce 900ms). Rota **/anotacoes** (grátis, isFreeDashboardPath, lista module_notes) + nav (NotebookPen). 
- **Streak/badges**: badges-defs +streak em BadgeStats + badges "Comprometimento"(streak>=7,ouro) e "Inabalável"(>=30,diamante). badges.ts computeStats computa streak de daily_activity.
- **ADMIN Saúde da Plataforma** `/admin/saude`: `lib/admin-health.ts` getPlatformHealth (checks: Supabase latência, Stripe balance.retrieve, webhook secret, Resend key, domínio HEAD; métricas users/subs/trial; capacidade %; engajamento 7d active/horas/min; insights). Página: carinhas emoji 😄😬😵 por status, **corações de vida** (1 por serviço OK, pulse animation), barras, métricas, insights. Nav admin +Saúde (Activity).
- **WEBHOOK STRIPE CONFIRMADO ATIVO**: https://www.pytrack.com.br/api/stripe/webhook registrado enabled 9 eventos (via Stripe API webhook_endpoints). ✅
- **DOCS negócio/segurança**: docs/SEGURANCA.md (análise avançada B+), micro-saas.md (32 perguntas respondidas), docs/Guia-SaaS-PyTrack.pdf (12pg, pandoc+tectonic, com pitch). Movidos BACKUP/CAPACITY/MONITORING p/ docs/.
- **COMMIT+PUSH**: git remote `dados`=estevam5s/pytrack (público, .env gitignored). Commit+push main OK (19af763). Tags v1.1.3+v1.1.4 pushed via git. ⚠️ api.github.com REST inacessível do ambiente (HTTP 000) — releases formais pendentes (tags existem, criar release pelo tag depois).

**Carreira Personalizada IA (Suprema) + docs + hero (jun/2026):**
- **CARREIRA PERSONALIZADA (Suprema)**: tabela `career_plans` (user_id PK, answers jsonb, plan text, recommended_tracks text[], RLS own). `lib/career-plan-actions.ts`: generateCareerPlan(answers) via chatComplete (system pede Markdown 4 seções + linha final "TRACKS: id1,id2..." parseada p/ recommended_tracks, fallback extrai ids citados), getCareerPlan, clearCareerPlan. `components/trilhas/career-plan-panel.tsx` (não-Suprema=upsell; Suprema sem plano=Questionnaire 6 campos objetivo/area/nivel/tempo/prazo/contexto; com plano=trilhas recomendadas ordenadas+plano colapsável+Refazer). Montado no topo de /minhas-trilhas (getCareerPlan no Promise.all). /evolucao mostra plano+trilhas recomendadas se existe (Compass). /precos Suprema +"Carreira Personalizada com IA". Verificado RLS anon→[].
- **/docs +5 seções técnicas** (app/(site)/docs): Arquitetura (route groups/middleware/edge/realtime), Modelo de dados (tabelas+RLS), IA & BYOK (parser tolerante, fallback cascata), API & Webhooks (pytk_live_, stripe webhook, /api/broadcast), Performance & Cache (unstable_cache, geradores memória, índices). Ícones Network/Database/Cpu/Webhook/Gauge.
- **HERO redesign** (estilo nodejs.org): fundo limpo bg-[#0a0a0f] + bg-grid opacity-40 + blur discreto (era blob primary/20 forte). Mockup dashboard SUBSTITUÍDO por `components/site/code-showcase.tsx` (editor animado: 4 abas Python FastAPI/Pandas/ML/Async, syntax highlight simples por regex keywords/strings/comments, animação de digitação setInterval, botão Copiar, cursor pulsante). hero-section.tsx usa CodeShowcase. Cards flutuantes atualizados ("Corrigido por IA nota 95", "Roda no navegador"). dashboard-mockup.tsx órfão (não usado). Cards flutuantes "Corrigido por IA"/"Roda no navegador" REMOVIDOS (pedido). **Logo+texto "PyTrack" acima do h1 REMOVIDO** + hero subiu (py-10 lg:py-14, era py-16/24); Image import removido de hero-section.

**Carreira plan UX v2**: career-plan-panel.tsx — plano agora em **MODAL centralizado** (componente Modal interno), markdown RENDERIZADO via `<Markdown>` (components/content/markdown.tsx, react-markdown+remark-gfm, classe .markdown tem 28 regras CSS) tanto no modal quanto /evolucao (era cru). Questionário também em modal. Opção **"Prefiro escolher"** (dismiss localStorage pytrack-career-dismissed → barra discreta "escolhendo livremente" + "Quero plano com IA").

**REPO/RELEASES**: este dir É repo git, remote `dados`=estevam5s/pytrack (mesmo repo do APK mobile). User quer release GitHub a cada deploy, sempre próxima versão. Criada **v1.1.0** (estava só v1.0.0 com APK) com changelog das features. Processo: ao deployar, criar release incrementando versão via API /repos/estevam5s/pytrack/releases.

**Respostas a perguntas do user (jun/2026)**:
- `BACKUP.md`/`CAPACITY.md`/`MONITORING.md` MOVIDOS p/ `docs/` (seguro: só MENCIONADOS como texto em admin/banco/page.tsx e comentário em lib/logger.ts, NÃO importados; refs de texto atualizadas p/ docs/). docs/ já tinha datafast/stripe/tasks.md.
- ⚠️ **`doc/Conteudos` NÃO pode ser removido**: lib/content/registry.ts lê o CORPO markdown das lições em runtime via `readFile("doc/Conteudos/...")` (manifest.ts mapeia file: doc/Conteudos/...). Tabela contents tem metadata (86 rows) mas o texto das aulas vem dos .md. Remover quebraria o conteúdo das lições.