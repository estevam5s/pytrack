---
name: plan-features
description: "Features por plano (jun/2026): Roadmaps grátis, Desafios Diários Essencial, Planejamento Completo, Construa SaaS Suprema + IA; Resend pendente"
metadata: 
  node_type: memory
  type: project
  originSessionId: e80488b4-200b-45f2-a1a1-9c9739d237ee
---

**Admin IA + tabs + bot submódulo (12/jun/2026):**
- **/admin/ia** (gerenciar toda IA): `lib/admin-ai.ts` getAiAdminData (providers Nvidia/OpenRouter por env key, CONFIGURED espelha lib/ai/openrouter.ts MODELS, AI_FEATURES onde IA é usada, getOpenRouterModels fetch openrouter.ai/api/v1/models SEM auth=338 modelos+pricing, uso de ai_usage[user_id,day,count]). `components/admin/model-icon.tsx` ModelIcon provider→cdn.simpleicons.org/{slug}/{color} onError→inicial. `components/admin/ai-models-browser.tsx` busca+filtro+só-grátis+paginação+copia ID. Nav admin +Gerenciar IA(Bot).
- **TABS removidas**: /exercicios tirou "Com solução" (só PracticeExercisesView). /perguntas-carreira-python tirou "Simulador"+"Carreira" (só QuestionsView).
- **FIX /admin/ia ERRO RENDER + MODELOS EDITÁVEIS (v1.3.0)**: erro "Server Components render error" era função `providerLabel` importada de model-icon.tsx ("use client") e chamada no server. Corrigido: extraído PROVIDERS/providerOf/providerLabel p/ `lib/ai-providers.ts` (neutro). **Modelos agora EDITÁVEIS pelo painel**: tabela `ai_settings`(id default/nvidia_models/openrouter_models jsonb, RLS sem policies só service role). `lib/ai/config.ts` getAiModels (unstable_cache tag "ai-models" revalidate 60, fallback DEFAULT_*_MODELS). openrouter.ts chatComplete usa `await getAiModels()` em vez de const MODELS. `lib/admin-ai-actions.ts` saveAiModels (ensureAdmin, upsert, revalidateTag). `components/admin/ai-models-config.tsx` (editar listas nvidia/openrouter add/remove, restaurar padrão). admin-ai.ts getAiAdminData lê configuredRaw de getAiModels. ai_settings vazio=usa defaults. ⚠️ NAV "Gerenciar IA" estava em ADMIN_NAV.items (linha 173) e admin-nav.tsx mapeia todos — user não via por CACHE do navegador (resolve com refresh).
- **PROVEDOR IA + TESTE + FIXES (v1.3.1)**: ai_settings +coluna `provider`('auto'/'nvidia'/'openrouter'). config.ts resolveProvider(auto=pela env NVIDIA_API_KEY). openrouter.ts chatComplete usa provider resolvido (base/key/models por provider; se nvidia sem key→openrouter). admin-ai-actions +saveAiProvider +testModel(modelId,provider→chatComplete override baseUrl/apiKey, mede latência). AiModelsConfig +seletor provedor (3 botões) +botão "Testar" por modelo (ModelRow mostra resposta+ms).
- **FIX DESAFIOS**: /desafios-diarios link ia p/ `/conteudos?ex=` mas /conteudos REDIRECIONA p/ /minhas-trilhas (page.tsx linha 6 redirect) → mudado p/ `/exercicios?ex=`.
- **SYNC PROGRESSO + STREAK + RELATÓRIO (v1.3.2)**: trilha-progress.tsx useEffectiveProgress = max(localStorage, serverPct) + setModuleProgressBySlug quando local>server (cross-device sync). +TrilhaListProgress p/ lista (trilhas-view s.moduleSlugs). minhas-trilhas list+[id] passam moduleSlugs{slug,total,serverPct}. **STREAK UNIFICADO**: `lib/streak.ts` getActivityDays(união exercise_completions+daily_activity)+streakFromDays. Aplicado em layout(sidebar) e desafios-diarios — antes divergiam (sidebar só exercícios, daily só estudo). **RELATÓRIO /evolucao**: `lib/evolution-report.ts` getEvolutionReport (user/activity/practice/learning/badges/**monthly[6 meses de daily_activity+exercise_completions]**) + `components/dashboard/report-export.tsx` exporta PDF(print)/DOC(html)/DOCX(lib docx)/TXT/JSON/XML. **+v1.3.3**: gráficos SVG puros no PDF (svgBars horas estudo+exercícios/mês) + tabela mensal em todos formatos + seção "Evolução mês a mês" na página /evolucao. **+v1.3.4**: comparativo este mês vs anterior (ArrowUp/Down/Minus + variação %) no topo /evolucao + radar SVG de proficiência por área no PDF (svgRadar, areas={radarData} de stats.byArea) + áreas em TXT/JSON/XML. **+v1.3.5**: monthly 12 meses (era 6, page flex overflow-x) + aiSummary gerado por chatComplete (maxTokens 180, fallback não-IA) no topo do relatório+página. **IDE /ide**: `lib/ide-examples.ts` ~70 exemplos completos em 18 categorias (era 7 inline) no select agrupado + status bar estilo VS Code (gradiente, Ln/UTF-8/Python3.12/Pyodide) + nav newTab:true (abre nova aba). main 06bc6b8 + tag v1.3.5. **+v1.4.0 IDE VS CODE FULL**: /ide virou rota TOP-LEVEL `app/ide/page.tsx` (removida app/(dashboard)/ide; middleware já tinha /ide na lista protegida=login+grátis). PythonIDE +prop `fullscreen` → layout VS Code h-screen: title bar (run/reset/tema), activity bar (Files/Search/Run icons), explorer w-64 (#252526) com busca + árvore de exemplos por categoria colapsável (clicar=setCode) + main.py + GithubPushButton, tab bar main.py, editor CodeMirror height 100%, terminal #181818 (34%), status bar #007acc. lib/ide-examples.ts agora **115 exemplos** completos em ~25 categorias (era 7). nav newTab abre nova aba. main 715597f + tag v1.4.0. **+v1.4.1 IDE PRO**: `lib/ide-ui-themes.ts` 9 temas IdeUiTheme (cores de TODO o chrome: bg/panel/activity/title/tabbar/terminal/status/text/accent/hover + editorTheme sincronizado) Dark+/Light+/Monokai/Dracula/OneDark/GitHubDark/NightOwl/Solarized/Synthwave. PythonIDE fullscreen reescrito (splice via /tmp/fsblock): estado uiThemeId/panel/files[]/active/fontSize/wrap; useEffect sincroniza setThemeId=ui.editorTheme; MÚLTIPLOS ARQUIVOS+ABAS (newFile/switchTo/closeFile/downloadActive); activity bar 4 painéis (explorer arquivos/examples árvore/search/settings tema+fonte±+wrap); todas cores via style inline do ui theme; EditorView.lineWrapping p/ wrap. ide-examples.ts **166 exemplos** (~30 categorias). main 5912bd9 + tag v1.4.1. ⚠️ user pediu "mais de o dobro"/200 — temos 166 (posso +60).
- **FIX PROGRESSO TRILHA (v1.3.1)**: minhas-trilhas/[id] usava progress_percentage do SERVIDOR (desatualizado) mas conclusão real está em localStorage `pytrack-lessons-{slug}` (igual bug do XP sidebar). `components/dashboard/trilha-progress.tsx` (TrilhaOverall/ModuleStatusIcon/ModulePctLabel) computa do localStorage com serverFallback. m.lessons.length = total.
- **FIX SCROLL LEITOR**: reader-shell.tsx 3 colunas agora scroll independente em lg+ (root lg:h-[calc(100dvh-8.5rem)] lg:overflow-hidden, cada coluna lg:overflow-y-auto) em vez de rolar o site inteiro. Tirou sticky top-20.
- **BOT SUBMÓDULO**: repo estevam5s/pytrack-bot já existia. bot-telegran/.git restaurado do backup /tmp; add README badges+`.github/workflows/ci.yml`(Node 18/20/22 typecheck)+LICENSE→push pytrack-bot. Repo principal: git rm -r --cached + git submodule add URL pública→.gitmodules SEM token. main 165789a + tag v1.2.9. ⚠️ bot remote resetado p/ URL limpa antes do submodule add (evita vazar token no .gitmodules).

**Features por plano + IA SaaS + Resend (jun/2026).**

- **GRÁTIS — Roadmaps de Carreira**: `/roadmaps` (app/(dashboard)/roadmaps, isFreeDashboardPath). Reusa TRILHAS (exceto suprema) como roadmaps estilo ViraDev (ícone/accent, etapas/horas/tópicos, link→/minhas-trilhas/[id]). CTA plano personalizado→/planejamento. Nav "Roadmaps" (Map, grupo Carreira).
- **ESSENCIAL — Desafios Diários**: `/desafios-diarios` (gated essencial via getUserTier+tierAtLeast, senão upsell). Desafio determinístico por dia (dayIndex % practice_exercises), streak (dias consecutivos com exercise_completions), últimos 7 dias, links /conteudos?ex=. Nav "Desafios Diários" (Flame, Estudar). default requiredTierForPath=essencial.
- **COMPLETO — Planejamento**: `/planejamento` (gated completo, +COMPLETO_ONLY). `components/planning/planning-hub.tsx`: 2 tabs — Plano de Estudos&Carreira IA (form goal/level/hours/career → `lib/planning-actions.ts` generateStudyPlan via chatComplete OpenRouter, gera markdown 4 partes: plano estudos/semanal/guia carreira/metas) + Planejamento Financeiro (calculadora freelance R$/h × horas, faixas salariais BR). Nav "Planejamento" (CalendarRange, Carreira).
- **SUPREMA — IA no Construa SaaS**: `lib/saas-actions.ts` generateBoilerplate (chatComplete gera código REAL adaptado à stack, markdown). Botão "Gerar boilerplate com IA" + modal copy em saas-roadmap.tsx. ⚠️ chatComplete usa `maxTokens` (NÃO max_tokens) e ChatOptions.
- **/precos**: features visíveis vêm do **AnimatedPricing plans** (linha ~150, NÃO do PLANS array linha 19). Atualizadas: Essencial+Desafios, Completo+Plano IA/semanal/guia/financeiro, Suprema+Construa SaaS. Descrição menciona Roadmaps grátis. Verificado no ar.
- **globals.css**: +classe `.input` (@layer components).

**RESEND — ATIVO ✅ (jun/2026)**: domínio **pytrack.com.br VERIFIED** no Resend (região sa-east-1 São Paulo; DKIM resend._domainkey.pytrack.com.br + SPF/MX em send.pytrack.com.br + DMARC _dmarc). RESEND_API_KEY=<RESEND_API_KEY_ROTACIONAR> (restricted send-only) + RESEND_FROM="PyTrack <nao-responda@pytrack.com.br>" no **Vercel** (prod/preview/dev) + .env. `lib/email.ts` usa Resend HTTP API se RESEND_API_KEY setado (welcome/transacional). **SMTP do Supabase TROCADO p/ Resend** (Management API config/auth: smtp_host=smtp.resend.com, port 465, user=resend, pass=API_KEY, smtp_admin_email=nao-responda@pytrack.com.br, sender PyTrack) — confirmação de email agora via Resend (resolve spam). Template confirmação personalizado (3294 chars) PRESERVADO. Testado: signup→confirmation_sent_at preenchido sem erro SMTP; envio externo via API retornou id. ⚠️ key send-only não lista/verifica domínios (precisa painel ou key full-access). ⚠️ key COMPROMETIDA no chat (rotacionar).

**DEBUG/Bugs + DESAFIOS + Plano de Carreira (jun/2026)**:
- **DEBUG (Essencial)** `/debug`: `lib/bugs-data.ts` gerador determinístico **3328 bugs** (52 contextos × 64 padrões reais: race condition/N+1/deadlock/memory leak/split-brain/etc, categorias código/arquitetura/banco, dificuldade fácil→expert, XP 35-250, tech lucide). `components/bugs/bugs-explorer.tsx` (filtros dificuldade/categoria/completados, busca, paginação 24/pg, sugerido, resolver→solveItem). Ranking lateral XpRanking. Gated essencial.
- **DESAFIOS (Essencial)** `/desafios`: `lib/challenges-data.ts` **2040 desafios** (40 variantes × 51 padrões: strings/arrays/algoritmos/data science/async/web/ML/etc, ícones tech Python). `components/challenges/challenges-explorer.tsx` (desafio do dia, filtros dificuldade/tópico, ícones tech, resolver). Ranking lateral. Gated essencial.
- **Infra**: tabela `user_solved` (user_id/kind/item_id/xp unique, RLS read público+insert dono; anon INSERT→401). `lib/solved-actions.ts` (solveItem upsert+community_add_xp, getSolvedIds, getSolvedRanking agrega xp por kind). `components/shared/tech-icon.tsx` (mapa lucide), `xp-ranking.tsx`.
- **PLANO DE CARREIRA (Completo)** `/plano-carreira`: tabela `career_entries` (kind/title/description/entry_date, RLS own). `lib/career-actions.ts` (addCareerEntry/deleteCareerEntry/weeklyCheckin via IA). `components/career/career-hub.tsx` (stats + tab Coach=check-in semanal IA + tab Evolução=timeline conquista/feedback/1a1/skill/visibilidade). Gated completo (+COMPLETO_ONLY).
- Nav: Debug(Bug)+Desafios(Swords) grupo Estudar, Plano de Carreira(Compass) Carreira. /precos: Essencial +Desafios/Debug, Completo +Plano de Carreira (AnimatedPricing). default requiredTierForPath=essencial cobre /debug /desafios.