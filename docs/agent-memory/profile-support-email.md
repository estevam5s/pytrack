---
name: profile-support-email
description: "Perfil avançado (capa/skills/conexões), suporte com presença/realtime/email, tutorial no banco, greeting/SMTP"
metadata: 
  node_type: memory
  type: project
  originSessionId: e80488b4-200b-45f2-a1a1-9c9739d237ee
---

**jun/2026 — pacote perfil/suporte/email/tutorial.** SQL em `supabase/profile-and-support-schema.sql` (aplicado). Token Management ROTACIONADO: novo é `<SUPABASE_MGMT_TOKEN_ROTACIONAR>` (o antigo `sbp_3634...` deu 401). Ver [[stripe-billing]] [[trilhas]] [[community]].

- **users_profile** ganhou: headline, bio, location, website, github_url, linkedin_url, cover_url, skills (text[]), tutorial_done (bool), last_seen_at. Função `touch_last_seen(uid)` (presença). Bucket `profile-covers` (público, RLS own-folder).
- **Tutorial**: `components/dashboard/onboarding-tour.tsx` reescrito — salva no banco via `completeTutorial()` (onboarding-actions.ts) em vez de localStorage; layout passa `initialOpen={!profile.tutorial_done}`; ao clicar "Próximo" navega p/ a rota do passo (router.push). Não reaparece em outro login/dispositivo.
- **Saudação /inicio**: era calculada no servidor (UTC) → bug "Bom dia" às 22h. Agora `components/home/greeting.tsx` (client, hora local).
- **/sobre**: stats "8 trilhas" → 16; `ContactForm` (email) + `ContactChannels`.
- **Stack Udemy** (`udemy-view.tsx`): gráfico Recharts com labels gigantes sobrepostos → tokeniza categorias por vírgula em techs individuais (top 10) + barras HTML legíveis com contagem.
- **E-mail (Gmail SMTP)**: `lib/email.ts` (nodemailer, service gmail, strip espaços da senha). Envs `GMAIL_USER`, `GMAIL_APP_PASSWORD`, `CONTACT_TO`. Usado em: suporte (avisa admin), reply admin (avisa user), contato /sobre. ⚠️ **A senha de app fornecida foi REJEITADA pelo Gmail (535-5.7.8 BadCredentials)** — precisa: 2FA ativo na conta + gerar novo app password. Código pronto, só falta credencial válida.
- **Suporte** (`/suporte`): status online/offline do admin (last_seen_at < 5min), categorias de chamado (SupportForm withCategory), thread realtime (`support-thread.tsx` via supabase channel), reply admin manda email.
- **Perfil**: edit dialog ganhou capa (upload profile-covers), headline, skills (tags). Type UserProfile + ProfileInput += headline/cover_url/skills.
- **Perfil público** `/comunidade/perfil/[id]`: capa, avatar, headline, skills, contadores (conexões/seguidores), **Conectar** (community_connections: pending/accepted, `lib/community/connections.ts`) e **Seguir** (toggleFollow existente) estilo LinkedIn. `components/community/profile-actions.tsx`.

**Chat IA + navbar + home (jun/2026).** SQL `supabase/chat-schema.sql` (aplicado): `chat_conversations` (user_id null p/ anon, anon_id, status bot|waiting_human|human|closed) + `chat_messages` (role user|assistant|admin|system); RLS select own (logado), escrita só service role. **Widget de chat** `components/chat/chat-widget.tsx` (só no site, montado em `app/(site)/layout.tsx`, NÃO no dashboard): botão flutuante cobra SVG canto inf. direito, IA "Py" via `/api/chat` (OpenRouter + `lib/chat-knowledge.ts`), markdown preview (`.chat-md` em globals), detecta logado/anônimo (anonId localStorage), escalona p/ humano (login ou nome/email guest), mostra admin online (last_seen<5min), polling quando waiting_human/human. **Admin** `/admin/chat` (nav Admin) vê TODAS as conversas (anon+cliente) e responde (`admin-chat-reply.tsx`, actions adminReplyChat/setChatStatus). **Navbar** estilo Node.js: `SiteSearch` (⌘K, busca rotas), `LangToggle` PT/EN via Google Translate (`language-provider.tsx`, cookie googtrans, CSP liberou translate.google/googleapis/gstatic, banner escondido em globals), ícone GitHub → github.com/PyTrackOrganization. **/inicio**: `AnimatedPython` (cobra SVG animada framer-motion) no hero. **Home site**: `FounderSection` (foto `public/founder.jpg` baixada do LinkedIn 800x800 + bio Estevam) antes dos depoimentos. **IDE temas/snippets + PWA + GitHub + perfil capa (jun/2026).** **Bug capa perfil corrigido**: profile-view não renderizava cover_url (era gradiente fixo) → agora `bg-cover` com cover_url + headline + skills exibidos. **/suporte**: redireciona admin→/admin/mensagens (admin é o suporte); nav item Suporte ganhou `userOnly:true` (escondido p/ admin via filtro sidebar). **IDE /ide**: `lib/ide-themes.ts` (32 temas via @uiw/codemirror-themes-all: dracula default, tokyo night, nord, monokai, etc), `lib/ide-snippets.ts` (~45 snippets Python por categoria), seletores de tema+snippets na toolbar; botão "Salvar no GitHub" (cria repo). **IDE↔exercícios**: ai-exercise-review tem "Abrir na IDE" (localStorage `pytrack-ide-pending` → IDE carrega). **GitHub**: `lib/github.ts` (saveGithubToken valida PAT + guarda username; createGithubRepo cria repo+arquivos via Contents API); colunas `github_token`/`github_username` em users_profile; `/configuracoes/github` (GithubConnect) p/ salvar PAT (scope repo); `components/github-push-button.tsx` reusável em IDE/exercícios/projetos. **Projetos /meus-projetos**: `ProjectSubmit` (enviar código → `analyzeProject` IA avalia requisitos + nota + GitHub). **PWA**: `app/manifest.ts` + `public/sw.js` + `components/pwa-install.tsx` (banner no site + botão no sidebar dashboard, beforeinstallprompt). ⚠️ **5000+ exercícios = batch script** (hoje 2462; gerar via scripts/generate-exercises — não feito neste turno).

**SEO rich + segurança + trilha grátis + conteúdo (jun/2026).** **SEO/Google**: add JSON-LD `Course` (rich result curso, offers R$10) + `FAQPage` (4 Q&A) no app/layout.tsx (já tinha Organization/WebSite SearchAction/SiteNavigationElement/sitemap/admin SEO). ⚠️ HONESTO: sitelinks/rich results do Google são automáticos, dependem de indexação (Search Console — precisa login do user) + autoridade/tempo; não dá p/ forçar via código. Structured data agora maximizado + validado live (Course+FAQPage+Organization+WebSite presentes). **Segurança**: `/.well-known/security.txt` (RFC 9116, app/.well-known/security.txt/route.ts). Rate limiting login/API/chat/comentários por IP + RLS + CSP + HSTS + X-Frame DENY + nosniff já robustos; DDoS real = infra (Vercel Firewall/Cloudflare, não código). **Trilha grátis** primeiros-passos 21→30 tópicos, adModules 6→8, adLessons 70→95. **Conteúdo ecossistema** +4 módulos (Streamlit, Web Scraping Playwright, Observabilidade OpenTelemetry, Qualidade mypy/Ruff) via scripts/gen-ecosystem-content.mjs → 56 ext, 86 contents, 111 lições; SQL contents_extra aplicado. ⚠️ LangChain/OpenCV/WebSockets já existiam (evitar dup slug). **Enrich perguntas**: 60 feitas, lote 400 background. ⚠️ aplicar SQL Mgmt API: usar `python open()` não `cat` (PATH quebra /usr/bin/cat).

**5269 exercícios + cookie + contato (jun/2026).** **Exercícios 2462→5269** (>5mil, todos títulos únicos, 81 categorias): geradores combinatórios `scripts/expand-exercises-v2..v6.mjs` (v2 padrões×subjects+variantes-teste; v3-v6 TASK×DOMAIN combinatório, domínios variados por lote, ex_id HX/JX/KX/LX/MX, source expansao-v2..v6, insert REST lotes 200). IDE↔exercícios+GitHub+IA já existiam. **Cookie consent LGPD** `components/cookie-consent.tsx` (banner inferior, localStorage pytrack-cookie-consent, aceitar/personalizar/recusar, toggles necessary/analytics/marketing, integra GA Consent Mode gtag consent update) montado em site layout. **Rota `/contato`** `app/(site)/contato/page.tsx` (ContactForm que já envia email via Gmail SMTP + canais diretos + info) no footer + sitemap. **Projetos**: site /projetos JÁ tem detalhe `/projetos/[id]` (steps/features/learnings) + 21 projetos (feito antes); dashboard /meus-projetos tem ProjectSubmit (IA). ⚠️ enrich perguntas ainda parado em ~17 (lote 300 lento/travou) — rodar scripts/enrich-questions.mjs em lotes.

**API tracks/ranking + acordeão + enrich perguntas (jun/2026).** **API +2 endpoints**: `/api/v1/tracks` (17 trilhas: id/title/level/plan/modules/topics) + `/api/v1/ranking?limit` (top por XP de community_profiles); add no playground + docs. TESTADOS ok. **Acordeão perguntas** (`/perguntas-carreira-python`): bug = open era state LOCAL de cada QuestionCard → vários abertos poluindo. CORRIGIDO: estado `openId` elevado ao QuestionsView (só 1 aberto), QuestionCard recebe open/onOpenChange; conteúdo com grid-rows-[1fr]/[0fr] + transition-all 300ms (fecha suave). **Respostas genéricas**: 1719 perguntas tinham só 26 valores distintos de application (genéricas idênticas). Criado `scripts/enrich-questions.mjs` (Nvidia llama-3.3-70b gera concept/application/mistakes/fix_fast/code ESPECÍFICOS por pergunta; coluna `enriched` bool p/ dedupe; via REST). Rodado lote — respostas agora específicas (variável≠vantagens≠o-que-é). ⚠️ 1719 total = rodar `node scripts/enrich-questions.mjs N` em lotes (300 rodando background). **Tutorial JÁ estava OK** (tutorial_done no banco, initialOpen, router.push na próxima) — não reaparece.

**Emails welcome/trial + API Suprema + playground + mini-projeto + notif sidebar (jun/2026).** **Email boas-vindas**: após confirmar conta (/auth/callback), envia welcomeEmailTemplate uma vez (coluna users_profile.welcomed). **Email aviso fim trial (3 dias antes)**: `/api/cron/trial-reminder` (Vercel Cron `vercel.json` 0 13 * * *, protegido CRON_SECRET ou x-vercel-cron) → trialEndingEmailTemplate p/ subs trialing c/ current_period_end em ~3d (dedupe users_profile.trial_reminded) + notif. Templates em email-templates.ts (welcome/trialEnding). **API → SUPREMA (R$46)**: gate mudado completo→suprema (api-keys.ts, configuracoes/api, manager). Novo endpoint **`/api/v1/progress`** (xp/level/modulesCompleted/recent). **Playground** `api-playground.tsx` em /configuracoes/api (testa API ao vivo, mostra cURL, chave só no browser). Docs `/docs/api` atualizada (Suprema, /progress, playground). **Mini-projeto** `mini-projetos/gestao-curso-python/` (gestao_curso.py — PyTrackClient stdlib urllib, classes Aluno/CursoPython, consome /me e /progress, relatório turma + export JSON; README). TESTADO com a chave do user `pytk_live_560b07...` → funciona (Estevam Souza). **Notif badge VERMELHO no sidebar**: sidebar-item badge bg-primary→bg-red-500; rota `/notificacoes` (page lista + marca lidas, `notifications-list.tsx`) add ao nav Conta com badge=notifCount; notifCount threaded layout→DashboardShell/MobileSidebar→SidebarContent. ⚠️ chave API do user apareceu no chat — rotacionar.

**Email pro + saldo admin + zh/ko + fix vaga-comment (jun/2026).** **BUG comentário vaga "column email does not exist" RESOLVIDO**: fn `community_ensure_profile` fazia `split_part(email,'@',1)` em users_profile (que NÃO tem email — está em auth.users) → falhava quando user sem perfil de comunidade comentava. Corrigido: pega email de `auth.users`. **Chinês+Coreano**: language-provider type Lang +zh-CN/ko, includedLanguages "en,es,zh-CN,ko,pt", LANGS +中文/한국어; LangToggle mostra flag (não código). **Email confirmação profissional**: Supabase exige SMTP custom p/ template → configurei SMTP Gmail no Supabase (config/auth via Mgmt API: smtp_host smtp.gmail.com, port '465' STRING, user/pass do Gmail, sender PyTrack) + template HTML `lib/email-templates.ts` (confirmEmail/cancellation/refund, dark, logo, gradiente) aplicado em mailer_templates_confirmation_content. **Email cancelamento**: webhook customer.subscription.deleted/updated(cancel_at_period_end) → `notifyCancellation` (email + notif, dedupe 12h). **Email reembolso**: requestRefund envia refundEmailTemplate + notif. **Painel saldo admin** `/admin/clientes`: stripeData += pendingAvailableOn (data libera, via balance_transactions), upcoming (próximas cobranças: subs active/trialing, total+nextDate); render 4 KPIs (disponível c/ aviso negativo, pendente c/ data, recebido, próximas cobranças). **Contexto trial**: user TEM trial 7 dias (STRIPE_TRIAL_DAYS=7) → novos assinantes ativam plano mas só são cobrados no fim do trial; 2 users trialing (cobram 16/06), 1 active pagou R$10 (pending, libera 05/07); saldo disponível -13,89 por reembolso anterior. User optou MANTER trial.

**DOCX nativo + fix rendimento Stripe (jun/2026).** **BUG rendimento RESOLVIDO**: pagamento R$10 (pi_3TfPJb, succeeded) não aparecia no painel admin pq a **chave restrita `rk_live_` NÃO tem permissão de ler balance/balance_transactions/charges** (só invoices/subscriptions/customers/payment_intents). `/admin/clientes` e `/admin/taxas` usavam balance.retrieve/charges.list/balanceTransactions.list → falhavam silenciosamente (catch). CORRIGIDO: agora usam **`paymentIntents.list`** (status=succeeded, amount_received) p/ a receita; balance em try/catch separado (saldo opcional); taxas: balanceTransactions com fallback p/ payment_intents (taxa estimada 3.99%+R$0.39, flag feesEstimated mostra aviso). Verificado: receita real R$120 (37 PIs, inclui o R$10). ⚠️ p/ taxa EXATA: dar permissões Balance/Balance transactions read à chave restrita no dashboard Stripe. **DOCX nativo**: lib `docx` (^9.7.1); `lib/resume/docx.ts` generateDocx (Document/Paragraph/TextRun, seções, cores do template, tabStops) → .docx REAL do Word; editor exportDocx (import dinâmico) substituiu o Word-HTML; DOC continua HTML. **+2 modelos currículo** (compact free, elegant suprema) = 8 templates total.

**Currículo + comentários + espanhol (jun/2026).** **Bug comentários**: CommentForm engolia o erro silenciosamente → agora exibe erro; createComment com ensure_profile RPC em try/catch (não quebra). Backend estava OK (insert+RLS+realtime funcionam). **Markdown nos posts**: post-card renderizava `whitespace-pre-wrap` (markdown cru nas vagas) → agora ReactMarkdown+remarkGfm (preview, classe chat-md). **Espanhol**: language-provider type Lang +es, includedLanguages "en,es,pt", LANGS export [pt/en/es]; LangToggle virou dropdown 3 idiomas (flags). **Lista seguidores/conexões clicável**: nova rota `/comunidade/conexoes` (Tabs conexões/seguidores/seguindo, cada user linka p/ perfil); contadores no /perfil agora linkam p/ lá; post-card avatar+nome linkam p/ `/comunidade/perfil/[id]`. **CURRÍCULO** (Completo R$19+): tabela `resumes` (jsonb data, RLS own, REST); `/curriculo` lista+CRUD (createResume pré-preenche do perfil, saveResume, deleteResume); editor `resume-editor.tsx` (form completo: pessoal/resumo/skills/exp/formação/projetos/idiomas + preview iframe ao vivo + 6 templates em `lib/resume/types.ts`, 3 deles SUPREMA-only: tech/executive/creative); render HTML `lib/resume/render.ts` por template; **export PDF (window.print), DOCX/DOC (Word-HTML blob), TXT (renderResumeText), compartilhar (navigator.share/clipboard)**. Nav Carreira + COMPLETO_ONLY + PROTECTED_PREFIX. ⚠️ **GIF de navegação NÃO É POSSÍVEL** neste ambiente (sem browser automation/screen recording) — informar user.

**Comunidade social + tradução (jun/2026).** **Comunidade**: post-card agora linka avatar+nome do autor → `/comunidade/perfil/[user_id]` (eram não-clicáveis); FollowButton já funcionava. **/perfil** mostra contadores sociais: busca counts de `community_follows` (seguidores=following_id, seguindo=follower_id) + `community_connections` (accepted, or requester/receiver) → ProfileView novo prop `social` renderiza "Conexões/Seguidores/Seguindo" + link p/ comunidade. **Tradução PT→EN corrigida**: language-provider.tsx reescrito — bug era `<div id=google_translate_element class="hidden">` (display:none impede o widget Google montar) → trocado p/ `sr-only`; setLang agora dispara o `.goog-te-combo` select diretamente (método confiável) com retry+fallback reload, em vez de só cookie+reload. **Painel admin tradução** `/admin/traducao`: status + lista TODAS as 16 rotas públicas c/ links PT e EN (via subdomínio translate.goog `www-pytrack-com-br.translate.goog/...?_x_tr_sl=pt&_x_tr_tl=en`) p/ verificar sem exceção + instruções (notranslate/translate=no p/ marcas). ADMIN_NAV += Tradução. Cobre todas rotas automaticamente.

**2FA trusted device + IDE highlight + AI quota (jun/2026).** **2FA "lembrar dispositivo"**: após verificar o código, cookie httpOnly `pytrack_td` (HMAC userId.exp, 30 dias, `lib/trusted-device.ts` Web Crypto edge-compat); middleware e signIn pulam o redirect /auth/mfa se o cookie for válido p/ o user. Checkbox "Confiar neste dispositivo por 30 dias" (default on) no /auth/mfa → `trustThisDevice` action. Resolve: não pedir código toda vez que loga. **Consistência marketing**: site dizia "16 trilhas" mas há **17** (add construir-llm) → corrigido em hero/sobre/site-search/trilhas/home. Componentes mortos `pricing-card.tsx`/`PricingCard.tsx` ("Python Pro", não usados) REMOVIDOS. **IDE highlight na home**: `ide-highlight.tsx` (zero-setup, mockup IDE com código+console, "rode Python no navegador") antes do showcase — gatilho de conversão p/ iniciantes. **AI quota por plano** (proteção custo/abuso): tabela `ai_usage` + fn `ai_hit`; `lib/ai/quota.ts` consumeAiQuota (limites diários free15/essencial80/completo200/suprema500; BYOK ilimitado; fail-open); aplicado no aiJson (lib/ai/actions.ts) → bloqueia c/ msg de upgrade ao exceder. ⚠️ pontos produção ainda pendentes (do review): load test autenticado, otimizar middleware (consulta Supabase toda rota), paginação 20k registros, Sentry/APM, métricas de funil — não feitos neste turno (AI quota foi o priorizado).

**Comunidade fix + showcase + entrevista IA (jun/2026).** **GA4 ativo**: NEXT_PUBLIC_GA_ID=G-NZV7GQ2B53 (env setada, gtag carrega). **Bug comunidade RESOLVIDO**: avatares não apareciam pq `community_profiles.avatar_url` era NULL (real está em users_profile) → `authorMap` agora faz fallback p/ users_profile (avatar_url + display_name; campo é display_name não full_name); SQL sincronizou histórico + trigger `sync_community_avatar` (after update users_profile). **Vagas não apareciam**: só no painel direito `hidden xl:block` (invisível <1280px) E não iam ao feed → `createJob` agora TAMBÉM cria community_post (category vaga) no feed; SQL criou post p/ vaga existente. **.env.example** atualizado (NVIDIA/Gmail/Management/GA/DataFast/TrustMRR/MAX_ACCOUNTS_PER_IP/LOG_WEBHOOK). **Showcase home**: `dashboard-showcase.tsx` (print public/dashboard.png em moldura browser + 4 destaques) na home. **Segurança anti-DDoS/brute-force**: login agora rate limit por IP (30/min) além de email (8/min); headers +COOP +x-dns-prefetch; SECURITY.md seção DDoS/DoS/Hydra (Vercel Firewall + Cloudflare recomendados, defense-in-depth). **Nova rota Suprema `/entrevista-ia`**: simulador de entrevista com IA interativo (config vaga/nível/experiência pré-preenchida do perfil → IA entrevista turn-a-turn, dicas, avaliação final nota 0-10); `interview-ai.tsx` + `interviewTurn` action (chatComplete); gate tierAtLeast suprema (senão paywall); nav Carreira; mostrado em /precos (Suprema feature + linha comparativo). ⚠️ **Search Console: ping Google/Bing DESCONTINUADO (404/410)** — envio do sitemap requer login no painel (verificação automática via GA4 mesma conta).

**API + GA + stack + changelog + IDE guard (jun/2026).** **CSP fix**: +translate-pa.googleapis.com + googletagmanager + google-analytics em script-src/connect-src (erro translate-pa resolvido). **IP limit**: MAX_ACCOUNTS_PER_IP agora env (padrão 2, setada=2). **Stack section** (prompts/stack.md): `tech-stack-section.tsx` na home (marquee CSS infinito `animate-marquee` add no tailwind, logos simpleicons das techs reais Next/React/TS/Tailwind/Supabase/Stripe/Vercel/Python/Pyodide/etc) — adaptado sem tsparticles. **Google Analytics**: `components/analytics.tsx` (GA4 via next/script, lê NEXT_PUBLIC_GA_ID) + metadata.verification.google (GOOGLE_SITE_VERIFICATION) — ⚠️ FALTAM os IDs do user (G-XXX e código Search Console) p/ ativar. **IDE anti-travamento**: guard `sys.settrace` com deadline 10s + limite 50M ops injetado antes do código do usuário (interrompe while True / código pesado sem freeze permanente). **Email real**: signUp checa MX records via node:dns resolveMx p/ domínios não-confiáveis (gmail/outlook/etc + .edu passam direto; sem MX → rejeita) + lista descartáveis ampliada. **API plataforma** (Completo+): tabela `api_keys` (hash sha256, prefix); `lib/api-keys.ts` (createApiKey/revokeApiKey, gate tierAtLeast completo) + `lib/api-keys-util.ts` (hashKey — separado p/ não quebrar "use server"); endpoint `GET /api/v1/me` (Bearer pytk_live_, rate 60/min, retorna xp/level/skills/modules); `/configuracoes/api` (ApiKeysManager) + docs públicas `/docs/api`. ⚠️ "use server" file não pode exportar função síncrona. **Changelog** site `/changelog` (versões v1.x dos PLATFORM_NEWS + roadmap) + footer. **Feedback cancelamento/reembolso**: tabela `cancellation_feedback`; RefundButton reescrito com form (motivo/melhoria/detalhes) → saveCancellationFeedback (+email admin) antes de requestRefund.

**SEO rich + notif sons + IP block (jun/2026).** **SEO/Google rich results**: app/layout.tsx JSON-LD expandido (@graph: Organization c/ sameAs+logo, WebSite c/ **SearchAction** p/ sitelinks searchbox, **ItemList de SiteNavigationElement** p/ sitelinks, EducationalOrganization); sitemap.ts recalibrado (prioridades + projetos, 57 urls). Rota admin **`/admin/seo`** (score SEO auditando home real via fetch, preview Google c/ sitelinks `google-preview.tsx`, analytics usuários/novos 7d-30d, ferramentas Search Console/PageSpeed/Rich Results). ADMIN_NAV += SEO. **Plano grátis dias exatos**: UpgradeBanner agora recebe `createdAt`, mostra dias restantes + data exata de expiração + barra de progresso (melhorou public/plano.png). **Notificações melhoradas**: SQL `users_profile.notif_prefs` jsonb + fn notify_user; `lib/notify.ts` (notifyUser respeita prefs); eventos: reply suporte→notif, conexão pedida/aceita→notif. **Sons**: `lib/notification-sound.ts` (Web Audio ding-dong, sem arquivo); NotificationBell toca som em INSERT realtime (respeita localStorage pytrack-notif-sound). `/configuracoes/notificacoes` (toggles por tipo + som, `notification-settings.tsx` + saveNotifPrefs). **Aviso fim de plano**: pg_cron `notify-trial-ending` (diário 12h) notifica grátis c/ <2 dias de trial. **IP anti-abuso**: tabela `signup_ips`; signUp captura IP (x-forwarded-for via headers()), bloqueia se ≥3 contas/IP (MAX_ACCOUNTS_PER_IP), registra após cadastro. **Atendimento personalizado Completo+**: feature ⭐ nos cards Completo/Suprema + banner "Atendimento personalizado a partir de R$19" em /precos.

**TrustMRR + backup anti-ban (jun/2026).** **TrustMRR**: `lib/trustmrr.ts` (getTrustMrr via API https://trustmrr.com/api/v1/startups/pytrack, Bearer TRUSTMRR_API_KEY env setada, cache 5min, rate 20/min); rota admin `/admin/trustmrr` (KPIs MRR/receita/rank/subs/growth + badge svg + perfil + techstack); badge `trustmrr.com/api/embed/pytrack?theme=dark` no footer do site (prova social). ADMIN_NAV += TrustMRR. API retorna: revenue{mrr,last30Days,total}, rank, activeSubscriptions, customers, growth30d, techStack, etc. **Backup anti-ban**: `scripts/backup-supabase.mjs` (exporta TODAS tabelas→JSON + storage files + manifest, via REST service role + Management API) — TESTADO: 38 tabelas/7.4MB (1719 perguntas, 2462 exercicios, 1364 projetos). `.github/workflows/backup.yml` (cron domingo 4h + manual → artifact GitHub 90 dias, fora do Supabase; opcional S3). `backups/` no gitignore. BACKUP.md atualizado secao 4.1. **Capacidade Supabase**: Free aguenta MAU(3000<50k) e DB(~200MB<500MB) MAS estoura storage(1GB) e egress(5GB) + sem PITR/SLA + compute nano fraco → NÃO p/ produção. Pro (US$25): 100k MAU, 8GB db, 100GB storage, 250GB egress, backups diários → aguenta 3000 com folga. Recomendado Pro a partir de ~300-500 pagantes.

**DataFast + reembolso 7d + capacidade (jun/2026).** **DataFast**: já inicializado (websiteId dfid_FSiAGhIh0Uf5yji54LpRj em datafast-init.tsx); agora atribuição de receita — checkout (`create-checkout-session/route.ts`) lê cookies `datafast_visitor_id`/`datafast_session_id` e injeta no metadata do checkout + payment_intent + subscription_data (vitalicio e recorrente); env `DATAFAST_API_KEY` setada. **Reembolso 7 dias**: server já validava (REFUND_WINDOW_DAYS=7, rejeita se ageDays>7). Adicionado: UI `/configuracoes/plano` calcula `daysSincePurchase` (subscription.created_at) e só mostra RefundButton se ≤7d (senão "prazo expirou, X dias"); webhook agora trata `charge.refunded`+`charge.dispute.created` → revoga acesso (cancela sub + delete). **IMPORTANTE: webhook Stripe `we_1TfPB2CB6Dz1wPei7mbNqSRG` (pytrack) reinscrito com charge.refunded+charge.dispute.created** (antes só tinha 7 eventos sem refund). Conta Stripe tem MUITOS webhooks (compartilhada c/ outros projetos). **/suporte admin**: já corrigido (redirect→/admin/mensagens + userOnly no nav + filtro sidebar). **Capacidade 3000 usuários**: `CAPACITY.md` — mix 10%Vitalicio/15%Suprema/25%Completo/20%Essencial/30%free = MRR R$40.950, ARR R$491k, +R$209k vitalicio. Veredito: Stripe/Vercel ok sem ação; Supabase suporta (REST/PostgREST faz pooling, não abre conexão direta/req; max_connections=60 só importa p/ conexão direta; DB ~166MB p/ 3000 users). Recomenda upgrade Supabase Pro. ⚠️ sandbox bloqueou curl (000) no fim — load test ao vivo não rodou, análise é matemática + stats reais do banco.

**CI fix + precos animado + chat mobile (jun/2026).** **CI/CD corrigido**: `next lint` (interativo, sem ESLint instalado) travava o CI. Instalado eslint@9 + eslint-config-next + @eslint/eslintrc; criado `eslint.config.mjs` (flat, extends next/core-web-vitals+next/typescript, desliga regras benignas: no-assign-module-variable/no-html-link-for-pages/no-empty-object-type/no-require-imports/no-anonymous-default-export); script lint = `eslint . --max-warnings=10000`. **lint+typecheck+build verdes**. ⚠️ git local remoto=analista-de-dados (não o repo PyTrack do CI) — user precisa sincronizar. **Precos animado** (`prompts/price.md`): `components/site/animated-pricing.tsx` (toggle mensal/anual + confetti + NumberFlow + cards 3D) com 3 planos (Essencial 10/8, Completo 19/15 popular, Suprema 46/37); deps: @number-flow/react, canvas-confetti, @radix-ui/react-switch; criados ui/switch.tsx + hooks/use-media-query.ts. **Chat mobile fullscreen**: container inset-0 h-[100dvh] no mobile, sm: volta flutuante. **features md**: +secao 14 STATUS DE IMPLEMENTACAO + roadmap.

**Projetos detalhe + news + admin taxas/banco (jun/2026).** **Projetos**: PROJECTS em lib/site-data ganhou `id/overview/features/steps/learnings/time` e DOBROU (12→21, níveis Iniciante→Desafio); ProjectCard vira Link p/ `/projetos/[id]` (página de detalhe c/ passo a passo, features, learnings). **Novidades** `/novidades` (changelog + roadmap, lib/news-data, free, grupo Conta). **Notícias Python** `/noticias-python` (fontes curadas + destaques; **Completo+** — em COMPLETO_ONLY + PROTECTED_PREFIX). **Admin /admin/taxas**: taxas reais Stripe (balanceTransactions: fee/net/gross) + tabela custos Supabase/Vercel/IA. **Admin /admin/banco**: stats via Management API (`SUPABASE_MANAGEMENT_TOKEN` env setada) — tamanho DB, tabelas+linhas+RLS, storage buckets, backups. ADMIN_NAV += Taxas/Banco. **2FA login auto-submit** ao completar 6 dígitos (/auth/mfa useEffect). **Perguntas** `/perguntas-carreira-python`: nova aba **Simulador** (flashcard aleatório por categoria/nível + placar) `interview-simulator.tsx`; QuestionsView já tinha busca/filtros (1719 perguntas c/ intro/concept/application/mistakes/fix_fast/code). ⚠️ Ampliar TODAS as 1719 respostas via IA = batch script (não feito neste turno). **Hero**: avatar group (prova social, fotos Unsplash) abaixo dos stats. Avatar radix instalado (@zxcvbn-ts já tinha sido p/ senha).

**Confirmação email + notif + admin colapsável (jun/2026).** SQL `supabase/notifications-and-settings-schema.sql` (aplicado): `notifications` (user_id, type, title, body, link, is_read; RLS own) + `site_settings` (singleton id=1: default_locale, maintenance, signups_enabled, announcement, contato, redes; RLS read público, escrita service role). **Confirmação de email ATIVADA** no Supabase (`mailer_autoconfirm=false` via Management API) → após cadastro vai p/ `/auth/verificar-email` (resend via `resendConfirmation` action); só acessa dashboard após confirmar. **Registro reforçado**: signUp valida confirmar-senha (campo password2), força mín 8, e-mail formato + bloqueia descartáveis + checa se já existe (admin.listUsers); `PasswordStrength` (zxcvbn) no form. **Sino de notificações** no header (`notification-bell.tsx`): contador vermelho, realtime (supabase channel INSERT), marcar lidas; layout conta `notifications` não lidas → `notifCount`. **Admin colapsável**: ADMIN_NAV em navigation.ts (hub + items); `admin-nav.tsx` (botão Admin expande subrotas: Configuração do site, Clientes & receita, Moderação, Mensagens, Chat); montado no SidebarContent só se isAdmin. **Nova rota `/admin/site`** (config global: idioma, contato, redes, anúncio, cadastros on/off, manutenção) — `saveSiteSettings` action. **/admin/clientes**: tabela com **avatar + nome** dos usuários (profiles join), busca por nome/email, + card **"Origem dos cadastros"** (signup_source). Token mgmt atual: <SUPABASE_MGMT_TOKEN_ROTACIONAR>.

**IA Nvidia + Gmail OK + features (jun/2026).** **IA migrada p/ Nvidia NIM**: `lib/ai/openrouter.ts` usa `NVIDIA_API_KEY` (env, setada) como provider padrão se existir → base `https://integrate.api.nvidia.com/v1`, modelos `meta/llama-3.3-70b-instruct`, `nvidia/llama-3.1-nemotron-70b-instruct`, `meta/llama-3.1-70b-instruct` (OpenRouter vira fallback). **TESTADO: IA responde** (chat/exercícios/consultor). **Gmail OK**: `GMAIL_APP_PASSWORD` nova (env) — `nodemailer.verify()` + envio reais funcionando. **Banner OG**: `app/opengraph-image.tsx` redesenhado (grid, gradiente, métricas, url) — metadata root c/ images explícitas (WhatsApp/Telegram/LinkedIn/Discord usam OG). ⚠️ OG: divs com >1 filho precisam `display:flex` (satori) senão build quebra. metadataBase=www.pytrack.com.br. **Registro**: select "Como você nos encontrou?" (LinkedIn/Instagram/etc) salvo em `users_profile.signup_source` (coluna add). **Trilhas**: nova **"Construir um LLM do zero"** (tier completo, módulo real `ext-construindo-um-llm-do-zero` — 9 lições: BPE/embeddings/attention/transformer/RoPE/treino PyTorch/LoRA/RLHF-DPO/vLLM; gerado via gen-ecosystem-content.mjs INSERINDO via json.dumps p/ escapar \n). Trilha **grátis "Primeiros Passos" ampliada** (21 tópicos, 6 mód/70 aulas/45h). **82 módulos** no total. **Sidebar** mais animado: SidebarItem c/ gradiente+scale no hover, badge do plano (TIER_BADGE) no topo, logo c/ glow. **Hero** redesenhado: print() em code, selos de confiança, stats em cards, CTA "Começar grátis". ⚠️ Credenciais Nvidia/Gmail apareceram no chat — rotacionar depois.
