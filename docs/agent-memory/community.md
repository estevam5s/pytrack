---
name: community
description: "Rota /comunidade (rede social do dashboard) — schema, RLS, realtime, componentes"
metadata: 
  node_type: memory
  type: project
  originSessionId: e80488b4-200b-45f2-a1a1-9c9739d237ee
---

Rota **/comunidade** = rede social educacional do PyTrack. Ver [[pytrack-platform]] e [[supabase-cron]].

**REFORMULAÇÃO LinkedIn standalone (jun/2026)**: /comunidade MOVIDA de (dashboard) p/ novo route group **`app/(rede)/`** — layout próprio (top nav estilo LinkedIn, sem chrome do dashboard), abre em **ABA NOVA**. `app/(rede)/layout.tsx` (auth redirect + RedeTopNav + LanguageProvider, fundo bg-surface-2/40, busca community_notifications unread). `components/rede/rede-topnav.tsx` (logo+busca, nav Início/Minha rede/Vagas/Notificações ícones+labels+badge, dropdown "Eu"→perfil/editar, botão "Voltar à plataforma"→/inicio, menu mobile). `components/rede/profile-card.tsx` (card perfil esquerda: cover+avatar+conexões/seguidores/XP). **Páginas** (todas /comunidade/*): feed (3-col reusa CommunityFeed+CommunityRightPanel+RedeProfileCard), perfil/[id] (cover/avatar/sobre/skills/atividade via getFeed authorId, ProfileActions ou Editar), eu (reusa ProfileView+ProfileEditDialog→updateProfile em users_profile), conexoes (tabs), vagas (getRecentJobs(30)+JobCard+CreateJobDialog), notificacoes (community_notifications+markNotificationsRead+timeAgo). **DELETADA** app/(dashboard)/comunidade/. navigation.ts NavItem +`newTab?` flag; sidebar-item.tsx target=_blank se newTab&&!locked. **getFeed() +3º arg `authorId`** opcional (filtra user_id). **Segurança OK**: middleware (lib/supabase/middleware.ts L146) gateia /comunidade COMPLETO+ (senão /assinar?upgrade=completo; unauth→/auth/login — verificado 307); admin client lê só campos públicos users_profile (name/avatar/cover/headline/bio/location/links/skills/level/xp — sem email/tokens); ações community usam client RLS. format.ts exporta `timeAgo`/`compact` (NÃO fromNow).

**PERFIL PROFISSIONAL LinkedIn-completo (jun/2026)**: SQL `supabase/community-profile-schema.sql` (aplicado via REST Management API, 201). **9 tabelas novas**: community_experiences (title/employment_type/company/company_logo_url/is_current/start_date/end_date/location/location_type/description/found_via/skills[]), community_education (school/degree/field_of_study/grade/activities/...), community_certificates (name/issuer/issue_date/expiry_date/credential_id/credential_url/skills), community_awards (title/issuer/award_date/image_url=LINK não upload/description/skills), community_languages (language/proficiency), community_test_scores (title/score/test_date), community_recommendations (author_id/target_id/relationship/body/status, unique author+target), community_skill_endorsements (target/endorser/skill), community_services (user_id PK/is_open/overview/services[]/affiliated_company/affiliated_company_logo/media_urls[]). +users_profile colunas **vanity_url** (unique lower) + profile_lang. RLS: SELECT público (true), escrita só dono (auth.uid()=user_id); recs por author; verificado anon INSERT→401, anon SELECT→200.
- **Actions** `lib/community/profile-actions.ts`: saveProfileItem/deleteProfileItem (6 seções via TABLES map, skills string→array), saveVanityUrl (slug sanitize+unique check), saveProfileLang, saveRecommendation (upsert), saveServices. **Queries** `lib/community/profile-queries.ts`: getProfileSections (paralelo all + autores recs + endorsements count + projects status=concluido).
- **UI**: `components/rede/section-dialog.tsx` (SECTION_FIELDS config 6 seções, form genérico text/textarea/date/checkbox/select), `profile-sections.tsx` (cliente, todos os cards: Serviços/Experiência/Formação/Certificados/Projetos/Competências+endorsements/Recomendações+RecommendButton/Prêmios/Notas/Idiomas, add/edit/delete se isSelf), `vanity-url.tsx`, `services-editor.tsx`, `onboarding.tsx` (modal 1ª visita localStorage pytrack-rede-onboarded).
- **Perfil page** `/comunidade/perfil/[id]`: **GATING SUPREMA** — ver perfil de OUTROS exige tier suprema (getUserTier+tierAtLeast; senão tela upgrade /assinar?upgrade=suprema; próprio perfil sempre OK). URL vanity exibida. Rota **`/in/[slug]`** (app/(rede)/in/[slug]) resolve vanity→redirect perfil. Página **/comunidade/perfil/[id]/servicos** (estilo LinkedIn: visão geral/serviços/empresa afiliada/mídia). **/precos** Suprema += "Apps+Bot" e "Ver perfis de outros membros da Rede". Onboarding no feed. Feed já ordena created_at desc (mais recentes). **Mensagens DM + Eventos + Certificados (jun/2026)**: SQL `supabase/community-extra-schema.sql` (aplicado REST 201). 4 tabelas: **community_messages** (sender/recipient/body/read, RLS: lê se sender ou recipient, insert sender, update recipient; REALTIME on), **community_events** + **community_event_attendees** (RLS público read, dono write; realtime events), **certificates** (user_id/trilha_id/trilha_title/recipient_name/level/hours/credential_code unique/issued_at, unique user+trilha; RLS SELECT público p/ verificação + insert dono).
- **Chat realtime**: `components/rede/chat-window.tsx` (createClient browser, channel postgres_changes INSERT community_messages, filtra par, otimista, markConversationRead). Páginas /comunidade/mensagens (lista conversas agrupadas) + /mensagens/[id] (ChatWindow). actions `lib/community/messages-actions.ts` (sendMessage RLS, markConversationRead). Botão "Mensagem" no perfil. Nav topnav +Mensagens +Eventos.
- **Eventos**: `components/rede/events-client.tsx` (CreateEventButton modal, AttendButton toggle, DeleteEventButton). /comunidade/eventos. actions `events-actions.ts` (createEvent/toggleAttend/deleteEvent). cover_url = link.
- **CERTIFICADOS de trilha**: `lib/certificates.ts` (getTrilhaCompletion server fonte-da-verdade via MODULES+moduleInTrilha+getProgressMap; issueCertificate idempotente — só se 100%, gera credential_code PTBK-{ano}-{6rand} unique; verifyCertificate público; getUserCertificates). `components/certificate/certificate-view.tsx` (visual dourado/navy estilo certificado.png: código, QR via api.qrserver.com→/certificado/{code}, nome, data/carga/nível/verificação, assinatura Estevam Souza). `certificate-button.tsx` (confetti, gera+modal, fetch /api/certificate/[code]). API route `app/api/certificate/[code]/route.ts`. **Trigger**: /minhas-trilhas/[id] quando overall>=100 mostra CertificateButton (busca cert existente via admin). **Página pública** `app/(site)/certificado/[code]` (verificação autenticidade, badge verde). Seção "Certificados PyTrack" no perfil. TESTADO end-to-end: inserção→página mostra "Certificado autêntico". QR aponta p/ verificação. /precos Suprema já cita ver perfis.

**Notif msg + share cert + BADGES (jun/2026)**:
- **Notificação de mensagem**: sendMessage agora insere em `notifications` (sino dashboard tem realtime nessa tabela, badge atualiza live) + `community_notifications`, via admin client (bypassa RLS). `components/rede/message-notifier.tsx` (montado no rede/layout): realtime community_messages filter recipient_id=eq.me → **Notification API do browser** (push) se permitido + router.refresh; pede permissão. Topnav: badge unreadMsgs no item Mensagens (layout conta community_messages read=false).
- **Compartilhar certificado**: `shareCertificateOnFeed(code)` em lib/certificates.ts (cria community_posts category=conquista com link verificação). certificate-button.tsx ShareButtons: "Compartilhar no feed" + botão LinkedIn (sharing/share-offsite?url=). 
- **BADGES (65, >46)**: tabela `user_badges` (user_id/badge_id unique, RLS read público + insert dono; anon INSERT→401 verificado). `lib/badges-defs.ts` BADGES: 17 trilhas (check trails.includes) + 22 tecnologias/áreas (areaPct>=50%, áreas de contents) + 8 exercícios (1..1000) + 7 XP (100..25k) + 6 social (posts/connections) + 5 conquistas (certs/nível). tiers bronze/prata/ouro/diamante (TIER_COLOR). `lib/badges.ts` (use server): computeStats (admin: users_profile xp/level, contents+progress→trails 100% via MODULES+moduleInTrilha + areaPct média, exercise_completions count, posts/connections/certs count), **evaluateMyBadges** (concede novas→user_badges + notifica em notifications, retorna {check} stripped), getEarnedBadges(userId). Página **/comunidade/badges** (evaluateMyBadges no load→concede, grid por categoria, locked/earned, progresso). Seção Conquistas no perfil (getEarnedBadges, emojis). Link "Conquistas" no menu Eu do topnav. evaluateMyBadges chamado também ao emitir certificado (import dinâmico).
**Sobre editar PNG do certificado por trilha**: mantida abordagem HTML/CSS dinâmica (CertificateView) — melhor que editar imagem (já é por-trilha c/ dados reais+QR); user concordou ("aquele esta bom").

**Ranking badges + mural diamante + SaaS + referral/notif fixes (jun/2026)**:
- **Mural diamante**: evaluateMyBadges, ao conceder badge tier=diamante, cria community_posts category=conquista automaticamente (mural de destaque no feed).
- **Ranking de badges**: getBadgeRanking(limit) em lib/badges.ts (agrega user_badges count+diamonds, join profiles). Exibido no topo de /comunidade/badges (Crown, 🥇🥈🥉, 💎count).
- **Notificações melhoradas**: `lib/notifications/actions.ts` (deleteNotification/clearAllNotifications/markAllNotificationsRead). notifications-list.tsx reescrito: filtros (Todas/Não lidas/Mensagens/Conquistas/Plano), agrupamento por data (Hoje/Ontem/semana/mês), botão X excluir por item, "Limpar tudo", cores/ícones por tipo.
- **REFERRAL fix**: recompensa convertia só no pagamento do indicado (webhook maybeRewardReferral). Agora `lib/referrals.ts` grantReferralReward(referredUserId) chamado no **auth/callback** (quando indicado CONFIRMA email): marca converted+reward_granted, crédito Stripe R$10 se indicador é cliente, SENÃO 1 mês cortesia (estende/cria subscription metadata referral_reward), notifica. Código f04468ea É válido (= 8 chars do uuid). STRIPE_REFERRAL_COUPON setado Vercel. ⚠️ Email confirmação ESTÁ enviando (confirmation_sent_at preenchido, nodemailer Gmail SMTP) — "não aparece" = entrega/spam do Gmail; verificar-email page tem ResendButton; sugerir provedor dedicado (Resend) p/ deliverability.
- **CONSTRUA SEU SAAS** (Suprema R$46): tabela `saas_projects` (user_id unique, name/idea/stack jsonb/steps_done[]/repo_url, RLS own). `lib/saas-builder.ts` (STACK_GROUPS 7 grupos: backend/frontend/database/auth/payments/deploy/ai; buildRoadmap gera fases adaptadas às escolhas). `lib/saas-actions.ts` (createSaasProject/toggleSaasStep/setSaasRepo/resetSaasProject). `components/saas/saas-wizard.tsx` (wizard multi-step stack picker) + `saas-roadmap.tsx` (roadmap c/ checkboxes, progresso, repo). Rota `/construir-saas` (gated suprema via getUserTier+tierAtLeast, senão upsell). Nav "Construa seu SaaS" (Rocket, grupo Estudar). billing-access +SUPREMA_ONLY+requiresSuprema (requiredTierForPath retorna suprema). Verificado RLS own (anon→[]), rotas 307.

**Banco (Supabase, ref zohqgnhymtqppgzlammv)**: schema em `supabase/community-schema.sql` (aplicado via Management API com token sbp_). 10 tabelas `community_*`: profiles, posts, comments, likes, saved_posts, follows, post_shares, reports, jobs, notifications. RLS habilitado em todas. Triggers mantêm contadores (likes/comments/saves/shares/reports), `updated_at`, XP/nível (função `community_level_from_xp`, `community_add_xp`), e criam notificações (like/comment/reply/follow/solution). `community_ensure_profile(uid)` cria o perfil sob demanda. Bucket Storage **community-post-images** (público, 5MB, jpg/png/webp). Realtime habilitado (publication supabase_realtime) para community_posts e community_comments. Seed dev: `supabase/community-seed.sql`. Script: `scripts/apply-community-schema.ts` (usa `SUPABASE_ACCESS_TOKEN` PAT — a Management API NÃO aceita service_role; service_role só PostgREST/Storage).

**App**: tipos em `types/community.ts`; níveis/categorias/filtros em `lib/community/levels.ts`; leituras server-only em `lib/community/queries.ts`; mutações (server actions) em `lib/community/actions.ts` (re-exportadas por posts/comments/likes/follows/jobs/notifications.ts); upload client em `lib/community/storage.ts`; helpers `lib/community/format.ts`. Componentes em `components/community/` (create-post-card, post-card, comment-section [realtime], community-feed [realtime de novos posts], community-sidebar+MobileFilterBar, right-panel, badges [LevelBadge/CategoryBadge/CommunityAvatar], job-card, create-job-dialog, report-dialog, follow-button, community-search). Página `app/(dashboard)/comunidade/page.tsx` (server, `force-dynamic`, layout 3 colunas responsivo, filtros via `?filtro=` e busca via `?q=`). Nav: item "Comunidade" (ícone Users) no grupo Estudar. Curtidas/saves/follows são otimistas; feed/comentários atualizam via Supabase Realtime + router.refresh.
